# sdlc-fix run - 2026-09-26T01:02:55.301Z

| File | Line | Layer | Origin | Resolution | Original marker | Applied text | Evidence |
|---|---|---|---|---|---|---|---|
| modules/auth/api.md | 102 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] The handler has no explicit `try/catch` around the `prisma.user.create` call; behavior on an unhandled database error (e.g. resulting HTTP status) is not established by the reviewed code. | (no change applied) | (no evidence cited) |
| modules/auth/api.md | 118 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] Whether this hard-coded base URL is intentional (e.g. local-only helper) or a defect for non-local deployments is not established by the code itself. | (no change applied) | (no evidence cited) |
| modules/auth/data-model.md | 18 | auto | GENERATE | deferred | **Schema:** [NEEDS CLARIFICATION] Not visible in the reviewed files. | (no change applied) | (no evidence cited) |
| modules/auth/data-model.md | 34 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] Column types (e.g. whether `id` is a UUID or auto-increment integer), nullability, and any additional columns (timestamps, etc.) are not established by the reviewed files. | (no change applied) | (no evidence cited) |
| modules/auth/data-model.md | 48 | auto | GENERATE | deferred | \| created_at \| [NEEDS CLARIFICATION] \| — \| Not referenced by any file in this dispatch's inputs. \| | (no change applied) | (no evidence cited) |
| modules/auth/data-model.md | 49 | auto | GENERATE | deferred | \| updated_at \| [NEEDS CLARIFICATION] \| — \| Not referenced by any file in this dispatch's inputs. \| | (no change applied) | (no evidence cited) |
| modules/auth/data-model.md | 61 | auto | GENERATE | deferred | \| [NEEDS CLARIFICATION] \| — \| — \| | (no change applied) | (no evidence cited) |
| modules/auth/data-model.md | 75 | auto | GENERATE | deferred | \| User \| [NEEDS CLARIFICATION] \| No retention or deletion logic for user accounts was found in the reviewed files. \| | (no change applied) | (no evidence cited) |
| modules/auth/domain-model.md | 58 | auto | GENERATE | deferred |  | (no change applied) | (no evidence cited) |
| modules/auth/domain-model.md | 63 | auto | GENERATE | rejected | [NEEDS CLARIFICATION] Relationships from `User` to other domain entities (e.g. a `Plan` owned by a user) are owned by other modules whose files are outside this dispatch's permitted read set. | (no change applied) | src/app/api/plans/route.ts; src/app/api/plans/[planId]/route.ts |
| modules/auth/overview.md | 20 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] A broader business rationale for why authentication exists (e.g. multi-tenant privacy or compliance requirements) is still not established. `README.md` is generic Next.js boilerplate with no finance-planner-specific content. The `docs/modules/planned-items/overview.md` and `docs/modules/web-ui/overview.md` and `docs/modules/persistence/overview.md` files named as inputs for this section are, as of this generation run, still unfilled skeleton documents (their content is the `[UNFILLED]` sentinel) belonging to a not-yet-run partition of the generation and could not be incorporated here. | (no change applied) | (no evidence cited) |
| modules/auth/overview.md | 34 | auto | GENERATE | rejected | - Define the internal behavior of `authOptions` (provider configuration, session strategy, JWT/callback logic, credential-verification logic). `authOptions` is imported from `@/lib/auth` in `src/app/api/auth/[...nextauth]/route.ts`, but that file is outside this dispatch's permitted read set (it is mapped to the `persistence`/`src/lib` scope, not `auth`). [NEEDS CLARIFICATION] Session strategy (JWT vs. database), token lifetime, and the exact credential-verification implementation are unconfirmed; `docs/modules/persistence/overview.md`, which could resolve this, is still an unfilled skeleton in this generation run. | (no change applied) | src/lib/auth.ts |
| modules/auth/overview.md | 57 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] No build manifest (e.g. `package.json`) was present in this dispatch's inputs, so the `next-auth`, `bcryptjs`, and `zod` package dependencies observed via `import` statements could not be cross-confirmed against a manifest as required by the dependency-grounding rule. | (no change applied) | (no evidence cited) |
| modules/auth/overview.md | 66 | auto | GENERATE | rejected | \| planned-items \| [NEEDS CLARIFICATION] `docs/modules/planned-items/overview.md`, named as an input for this section, is still an unfilled skeleton (`[UNFILLED]`) in this generation run; its dependency on `auth` sessions could not be confirmed here. \| | (no change applied) | src/app/api/plans/[planId]/items/route.ts; src/lib/requireUser.ts |
| modules/auth/overview.md | 67 | auto | GENERATE | rejected | \| web-ui \| [NEEDS CLARIFICATION] `docs/modules/web-ui/overview.md`, named as an input for this section, is still an unfilled skeleton (`[UNFILLED]`) in this generation run; its dependency on `auth` sessions could not be confirmed here. \| | (no change applied) | src/app/plans/page.tsx; src/app/plans/[planId]/page.tsx; src/app/login/LoginClient.tsx; src/app/register/page.tsx |
| modules/auth/overview.md | 68 | auto | GENERATE | rejected | \| persistence \| [NEEDS CLARIFICATION] `docs/modules/persistence/overview.md`, named as an input for this section, is still an unfilled skeleton (`[UNFILLED]`) in this generation run; whether/how it depends on `auth` could not be confirmed here. \| | (no change applied) | src/lib/db.ts |
| modules/auth/technical-concerns.md | 45 | auto | GENERATE | rejected | \| Database \| [NEEDS CLARIFICATION] The Prisma schema file (constraints) is outside this dispatch's permitted read set. \| | (no change applied) | src/app/api/auth/register/route.ts |
| modules/auth/use-cases/register-account.md | 83 | auto | GENERATE | rejected | \| Unhandled database error during `prisma.user.create` \| [NEEDS CLARIFICATION] No explicit `try/catch` exists around this call in `src/app/api/auth/register/route.ts`; resulting behavior is not established. \| | (no change applied) | src/app/api/auth/register/route.ts; src/app/register/page.tsx:27; src/app/register/page.tsx:29 |
| modules/auth/use-cases/sign-out.md | 30 | auto | GENERATE | deferred | - [ ] [NEEDS CLARIFICATION] Whether the session cookie is actually cleared is determined by NextAuth's internal `/api/auth/signout` flow, which is outside this dispatch's permitted read set; only the redirect step itself is grounded. | (no change applied) | (no evidence cited) |
| modules/auth/use-cases/sign-out.md | 39 | auto | GENERATE | deferred | \| 3 \| System \| — \| [NEEDS CLARIFICATION] NextAuth's internal sign-out flow at `/api/auth/signout` is not visible in this dispatch's permitted read set; it presumably clears the session and follows `callbackUrl` to `/login`. \| | (no change applied) | (no evidence cited) |
| modules/auth/use-cases/sign-out.md | 51 | auto | GENERATE | deferred | **Grounded note:** the redirect's base URL is the hard-coded literal `"http://localhost:3000"`, not derived from the incoming request. [NEEDS CLARIFICATION] Whether this is correct outside local development is not established by the code. | (no change applied) | (no evidence cited) |
| modules/auth/use-cases/sign-out.md | 66 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] | (no change applied) | (no evidence cited) |
| modules/persistence/behavior/credentials-authorization.md | 85 | auto | GENERATE | deferred | \| Database error during `findUnique` \| [NEEDS CLARIFICATION] No try/catch is present in `authorize()`; propagation behavior is not confirmed in this module's inputs. \| | (no change applied) | (no evidence cited) |
| modules/persistence/behavior/session-user-resolution.md | 66 | auto | GENERATE | rejected | [NEEDS CLARIFICATION] No try/catch is present in `requireUserId()`; propagation is not confirmed in this module's inputs. | (no change applied) | src/lib/requireUser.ts; src/app/api/plans/route.ts; src/app/api/plans/[planId]/route.ts; src/app/api/plans/[planId]/items/route.ts |
| modules/persistence/deployment.md | 19 | auto | GENERATE | deferred | **Deployment Type:** [NEEDS CLARIFICATION] Not stated in `src/lib/auth.ts`, `src/lib/db.ts`, or `src/lib/requireUser.ts`. The module is part of a Next.js application (per the top-level `README.md`, a `create-next-app` project); README.md's "Deploy on Vercel" section is default `create-next-app` scaffolding text and is not confirmed as this project's actual deployment target within this module's inputs. | (no change applied) | (no evidence cited) |
| modules/persistence/deployment.md | 29 | auto | GENERATE | deferred | \| `DATABASE_URL` \| string \| PostgreSQL connection string; `src/lib/db.ts` throws `"DATABASE_URL is not set"` at module load if absent \| [NEEDS CLARIFICATION] no example value present in inputs \| | (no change applied) | (no evidence cited) |
| modules/persistence/overview.md | 35 | auto | GENERATE | deferred | - Include the Prisma schema in the files available to this module; `src/lib/db.ts` imports the generated client from `@/generated/prisma/client` but the schema source (`schema.prisma`) is not part of this module's inputs. [NEEDS CLARIFICATION] Confirm the location of `schema.prisma` and include it in a future documentation pass. | (no change applied) | (no evidence cited) |
| modules/persistence/technical-concerns.md | 36 | auto | GENERATE | rejected | \| Database \| [NEEDS CLARIFICATION] Constraints (uniqueness, NOT NULL, etc.) cannot be confirmed without `schema.prisma` \| | (no change applied) | src/app/api/auth/register/route.ts; src/app/api/plans/route.ts |
| modules/persistence/use-cases/authenticate-with-credentials.md | 27 | auto | GENERATE | rejected | - [ ] A `prisma.user` row exists whose `email` matches the (lower-cased, trimmed) submitted email. [NEEDS CLARIFICATION] Additional preconditions (e.g. account "active" status) cannot be confirmed without the Prisma schema. | (no change applied) | src/lib/auth.ts |
| modules/planned-items/overview.md | 49 | auto | GENERATE | rejected | [NEEDS CLARIFICATION] `requireUserId()` (`@/lib/requireUser`) performs session/user resolution; per `generation.module-source-map` this file path falls under the `persistence` module (`src/lib`), though functionally it is an authentication concern; the true owning module could not be confirmed from this dispatch's inputs. | (no change applied) | src/lib/requireUser.ts; src/lib/auth.ts; src/lib/db.ts |
| modules/plans/api.md | 23 | auto | GENERATE | rejected | **OpenAPI Spec:** [NEEDS CLARIFICATION] No OpenAPI/Swagger specification file was present in inputs.code. | (no change applied) | src/app/api/plans/route.ts; src/app/api/plans/[planId]/route.ts; README.md |
| modules/plans/api.md | 114 | auto | GENERATE | rejected | \| 409 \| `Plan for this month already exists` \| The Prisma `create` call throws (caught generically as `e: any`), which the code comment attributes to a unique constraint on `(userId, year, month)`. The route does not inspect the Prisma error code before returning 409, so any create failure currently maps to this message - [NEEDS CLARIFICATION] whether other Prisma failures should map to a different status. \| | (no change applied) | src/app/api/plans/route.ts:49 |
| modules/plans/api.md | 152 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] Whether deleting a plan cascades to its items at the database level is not established by `src/app/api/plans/[planId]/route.ts` alone (it only calls `prisma.plan.delete`); the Prisma schema that would define `onDelete` behavior is not in this module's inputs. | (no change applied) | (no evidence cited) |
| modules/plans/data-model.md | 21 | auto | GENERATE | rejected | **Schema:** [NEEDS CLARIFICATION] No `schema.prisma` file was present in inputs.code for the plans module; column types below are inferred only from field usage in `src/app/api/plans/route.ts` and `src/app/api/plans/[planId]/route.ts`, not from a schema declaration. | (no change applied) | src/app/api/plans/route.ts; src/app/api/plans/[planId]/route.ts |
| modules/plans/data-model.md | 60 | auto | GENERATE | deferred | \| createdAt \| [NEEDS CLARIFICATION] \| - \| Not read/written directly on `Plan` in the manifest files; only `items.createdAt` is used for ordering \| | (no change applied) | (no evidence cited) |
| modules/plans/data-model.md | 72 | auto | GENERATE | deferred | \| userId \| User (auth module) \| [NEEDS CLARIFICATION] Not established by this module's inputs \| | (no change applied) | (no evidence cited) |
| modules/plans/data-model.md | 82 | auto | GENERATE | rejected | \| id \| [NEEDS CLARIFICATION] \| NO \| Primary key \| | (no change applied) | src/app/api/plans/[planId]/items/route.ts; src/app/api/plans/[planId]/route.ts; src/app/plans/[planId]/page.tsx |
| modules/plans/data-model.md | 101 | auto | GENERATE | rejected | [NEEDS CLARIFICATION] No retention policy, archival strategy, or soft-delete flag was observed; `DELETE /api/plans/{planId}` performs a hard `prisma.plan.delete`. | (no change applied) | src/app/api/plans/[planId]/route.ts |
| modules/plans/deployment.md | 41 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] No compute/storage sizing information (Dockerfile, k8s resources, hosting config) is present in inputs.code or inputs.references for the plans module. | (no change applied) | (no evidence cited) |
| modules/plans/deployment.md | 45 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] No scaling configuration was observed for this module. | (no change applied) | (no evidence cited) |
| modules/plans/technical-concerns.md | 29 | auto | GENERATE | rejected | \| Database \| [NEEDS CLARIFICATION] Constraint enforcement (e.g. the presumed unique index on `(userId, year, month)`) is inferred only from the `409` handling in `POST /api/plans`; the schema itself is not in this module's inputs \| | (no change applied) | src/app/api/plans/route.ts |
| modules/plans/use-cases/delete-plan.md | 34 | auto | GENERATE | deferred | - [ ] The plan row (and, per the route handler, only the plan row itself - see [NEEDS CLARIFICATION] on cascade behavior in data-model.md) no longer exists | (no change applied) | (no evidence cited) |
| modules/plans/use-cases/list-plans.md | 54 | auto | GENERATE | rejected | [NEEDS CLARIFICATION] `src/app/plans/page.tsx` does not appear to catch a database-query failure explicitly; behavior on a `prisma.plan.findMany` throw is not established by this module's inputs. | (no change applied) | src/app/plans/page.tsx |
| modules/web-ui/overview.md | 51 | auto | GENERATE | deferred | \| next-auth (external package) \| `getServerSession` is imported directly from `next-auth` in `src/app/page.tsx` to read the current session server-side. [NEEDS CLARIFICATION] No `package.json` was present in this module's manifest inputs to confirm the installed `next-auth` version. \| | (no change applied) | (no evidence cited) |
| overview.md | 21 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] `README.md` is generic Next.js `create-next-app` boilerplate and contains no finance-planner-specific business description (target users, organization, or regulatory context). No other reference documents were supplied to this dispatch, so the broader business rationale for the system is unconfirmed. | (no change applied) | (no evidence cited) |
| overview.md | 53 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] No build manifest (e.g. `package.json`) was present in this dispatch's inputs, so external package dependencies (`next-auth`, `bcryptjs`, `zod`, `@prisma/adapter-pg`, etc.) referenced across the module overviews could not be cross-confirmed against a manifest per the dependency-grounding rule. | (no change applied) | (no evidence cited) |
| overview.md | 62 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] The actual hosting environment, database hosting provider, and any deployment/CI configuration are not confirmed by this dispatch's inputs (`README.md` and the five module overview docs). | (no change applied) | (no evidence cited) |
| overview.md | 73 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] Whether the system is a monolith by deliberate architectural choice versus incidental (e.g. no separate service boundaries observed) is not addressed by any reference document in this dispatch's inputs. | (no change applied) | (no evidence cited) |
| technology-context.md | 60 | auto | GENERATE | deferred | - [NEEDS CLARIFICATION] Whether this project is actually deployed to Vercel in practice (as opposed to the README's generic `create-next-app` boilerplate guidance), and any project-specific deployment configuration, cannot be confirmed: `docs/modules/persistence/deployment.md` is unfilled at the time of this generation, and no other deployment-specific reference was supplied to this node. | (no change applied) | (no evidence cited) |
| technology-context.md | 75 | auto | GENERATE | deferred | [NEEDS CLARIFICATION] Regenerate or re-run this synthesis node once the modules above have been generated, so their content can be incorporated into the technology context. | (no change applied) | (no evidence cited) |
