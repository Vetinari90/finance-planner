# Review report

> Generated deterministically by `sdlc review-synthesize`.

## docs/cross-cutting/data-handling.md

- [completeness] The document's stated premise that the planned-items module overview was still an unfilled skeleton and could not be incorporated is stale: docs/modules/planned-items/overview.md is fully populated. Multiple downstream subsections (Authorization Model, Data Sensitivity, Data Classification) that depend on this premise omit information that is actually available in that document.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/overview.md is fully populated (Purpose, Responsibilities, Boundaries, Key Entities, Dependencies sections all have concrete content, e.g. Key Entities table lists PlannedItem with fields, and Responsibilities names title/amountCents/categoryId/note), not an unfilled skeleton. It is also one of the designated module-docs inputs for docs/cross-cutting/data-handling.md per docs/doc_struc.md.
  - location: (`docs/modules/planned-items/overview.md`) was still an unfilled skeleton document at
- [consistency] This document asserts the planned-items module overview was an unfilled skeleton and could not be incorporated, but docs/modules/planned-items/overview.md (in the same docs tree) is fully populated with Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, and Dependents content, contradicting this claim.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/overview.md is fully populated (Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, Dependents sections all present with concrete content), not an unfilled skeleton, contradicting this doc's claim.
  - location: (`docs/modules/planned-items/overview.md`) was still an unfilled skeleton document at
- [completeness] This section says whether planned-items re-verifies plan ownership 'could not be confirmed' because the planned-items overview was treated as an unfilled skeleton, but docs/modules/planned-items/api.md (a populated document) already documents the ownership check (prisma.plan.findFirst({ id: planId, userId } })) for the POST endpoint - material available in the input set was not incorporated.
  - confidence: high; agreement: 1
  - evidence: docs/modules/planned-items/api.md documents the POST /api/plans/{planId}/items endpoint's 404 error code as 'No plan with the given planId exists for the authenticated user (prisma.plan.findFirst({ where: { id: planId, userId } }) returns null)', which is the same ownership-check pattern used by the plans module.
  - location: [NEEDS CLARIFICATION] Whether the planned-items module (item-level CRUD under
- [consistency] This document claims item-level financial data could not be confirmed because the planned-items module overview is an unfilled skeleton, but that overview document actually names the concrete fields (title, amountCents, categoryId, note), contradicting the premise.
  - confidence: high; agreement: 1
  - evidence: docs/modules/planned-items/overview.md Responsibilities section states: 'Validate and persist new planned items (`title`, `amountCents`, optional `categoryId`, optional `note`) under a given plan, via POST /api/plans/{planId}/items.' - concrete field names are available.
  - location: overview is an unfilled skeleton document in this generation run.

## docs/cross-cutting/security.md

- [completeness] security.md is the recipe/template for threat-model.md and data-handling.md, but is missing the Audit Trail and Monitoring and Observability sections that both sibling documents generated from the same recipe include as required top-level sections.
  - confidence: high; agreement: 1
  - evidence: docs/doc_struc.md lists docs/cross-cutting/threat-model.md and docs/cross-cutting/data-handling.md with recipe: docs/cross-cutting/security.md, i.e. security.md is their template. Both sibling docs contain top-level '### Audit Trail' and '### Monitoring and Observability' sections (threat-model.md lines 51 and 74; data-handling.md lines 82 and 151). security.md itself has no Audit Trail or Monitoring and Observability section anywhere in its content, and neither term appears in the document.
  - location: ### Known Gaps and Follow-Ups
- [completeness] This line claims docs/modules/persistence/overview.md is still an UNFILLED skeleton and could resolve the session-strategy question, but that document is now fully populated and states the session strategy is jwt-based - material available in the current input set was not incorporated into security.md's answer.
  - confidence: high; agreement: 2
  - evidence: docs/modules/persistence/overview.md (not an UNFILLED skeleton) states under Responsibilities: 'Define authOptions (NextAuthOptions): a CredentialsProvider that verifies email/password against prisma.user with bcrypt.compare, a jwt-strategy session, jwt/session callbacks that propagate the user ID onto the session, and a custom /login sign-in page (src/lib/auth.ts).' This answers the 'session strategy (JWT vs. database)' question the line raises. Sibling docs generated later (docs/cross-cutting/threat-model.md line 21, docs/cross-cutting/data-handling.md lines 43-45) already cite this same source successfully.
  - location: - [NEEDS CLARIFICATION] The internal behavior of `authOptions` — provider configuration, session strategy (JWT vs. database), token lifetime, and the exact credential-verification logic — is imported from `@/lib/auth` but that file falls under the `persistence`/`src/lib` module scope, not `auth`, and is outside this dispatch's grounded read set. `docs/modules/persistence/overview.md` and `docs/modules/persistence/technical-concerns.md`, which could resolve this, are still `[UNFILLED]` skeleton documents from a not-yet-completed parallel work package at the time this document was generated.
- [consistency] Contradicted by the current doc tree: docs/modules/persistence/overview.md is not an '[UNFILLED]' skeleton - it is a complete document that explicitly states authOptions uses a 'jwt-strategy session', directly answering the session-strategy question this line calls unresolved.
  - confidence: high; agreement: 2
  - evidence: docs/modules/persistence/overview.md is a fully generated document (not an '[UNFILLED]' sentinel) and its Responsibilities section states: 'Define `authOptions` (`NextAuthOptions`): a `CredentialsProvider` that verifies email/password against `prisma.user` with `bcrypt.compare`, a `jwt`-strategy session, `jwt`/`session` callbacks that propagate the user ID onto the session, and a custom `/login` sign-in page (`src/lib/auth.ts`).' This directly resolves the session-strategy question (JWT, not database) that security.md flags as unresolved.
  - location: - [NEEDS CLARIFICATION] The internal behavior of `authOptions` — provider configuration, session strategy (JWT vs. database), token lifetime, and the exact credential-verification logic — is imported from `@/lib/auth` but that file falls under the `persistence`/`src/lib` module scope, not `auth`, and is outside this dispatch's grounded read set. `docs/modules/persistence/overview.md` and `docs/modules/persistence/technical-concerns.md`, which could resolve this, are still `[UNFILLED]` skeleton documents from a not-yet-completed parallel work package at the time this document was generated.
- [completeness] This line claims docs/modules/planned-items/api.md is still an UNFILLED skeleton, but that document is now fully populated and shows planned-items uses the same prisma.plan.findFirst({ id: planId, userId }) ownership-check pattern as plans - material available in the current input set that answers this gap was not incorporated.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/api.md (not an UNFILLED skeleton) documents, in its Error Codes table for POST /api/plans/{planId}/items: '404 | Not Found | No plan with the given planId exists for the authenticated user (prisma.plan.findFirst({ where: { id: planId, userId } }) returns null)' - the same userId-scoped ownership-check pattern used by the plans module.
  - location: - [NEEDS CLARIFICATION] Whether the `planned-items` module (item CRUD under `/api/plans/{planId}/items`) applies the same user-scoping and ownership-check pattern cannot be confirmed here: `docs/modules/planned-items/overview.md`, `docs/modules/planned-items/technical-concerns.md`, and `docs/modules/planned-items/api.md` are all still `[UNFILLED]` skeleton documents at the time this document was generated.
- [consistency] Contradicted by the current doc tree: the three named planned-items docs are not '[UNFILLED]' skeletons, and planned-items/api.md already documents the same `{ id: planId, userId }` ownership-check pattern used by the plans module - the very fact this line says cannot be confirmed.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/overview.md, technical-concerns.md, and api.md are all fully generated documents, not '[UNFILLED]' sentinels. planned-items/api.md's error-code table for POST /api/plans/{planId}/items states: '404 | Not Found | No plan with the given planId exists for the authenticated user (`prisma.plan.findFirst({ where: { id: planId, userId } })` returns null)' - the same ownership-check pattern used by the plans module.
  - location: - [NEEDS CLARIFICATION] Whether the `planned-items` module (item CRUD under `/api/plans/{planId}/items`) applies the same user-scoping and ownership-check pattern cannot be confirmed here: `docs/modules/planned-items/overview.md`, `docs/modules/planned-items/technical-concerns.md`, and `docs/modules/planned-items/api.md` are all still `[UNFILLED]` skeleton documents at the time this document was generated.
- [consistency] Contradicted by the current doc tree: docs/modules/planned-items/technical-concerns.md is not an '[UNFILLED]' skeleton - it documents an API-layer Zod rule requiring `amountCents` to be a non-negative integer, confirming server-side re-validation of the item amount that this line says is unconfirmed.
  - confidence: high; agreement: 1
  - evidence: docs/modules/planned-items/technical-concerns.md is a complete document (not '[UNFILLED]') whose Field Validation Rules table lists 'amountCents | number (int) | Yes | integer, minimum 0' as an API-layer (Zod `CreateItemSchema`) rule, confirming server-side re-validation of the item amount.
  - location: - [NEEDS CLARIFICATION] Whether the `planned-items` module re-validates the item amount server-side (the `plans` module's `technical-concerns.md` notes this "happens in the planned-items module, outside this manifest") could not be confirmed, since `docs/modules/planned-items/technical-concerns.md` is still an `[UNFILLED]` skeleton.

## docs/cross-cutting/threat-model.md

- [completeness] This line claims docs/modules/planned-items/overview.md is still an UNFILLED skeleton and therefore the planned-items enforcement pattern is unconfirmed, but that document is now fully populated and describes an ownership-enforcement rule for planned-item creation - material available in the current input set was not incorporated into this threat entry.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/overview.md is a fully populated document (Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, Dependents sections all filled), not an '[UNFILLED]' skeleton. Its Responsibilities section states: 'Enforce that a planned item can only be created under a plan owned by the authenticated user', which speaks directly to the enforcement-pattern question this line calls unconfirmed.
  - location: **Threat (authorization-bypass-by-omission):** `docs/modules/persistence/overview.md` explicitly documents that `requireUserId()` does **not** itself throw or redirect when no session is present - it only returns `null`, "leaving enforcement (e.g. returning a 401) to its callers." A route handler that fails to check for a `null` return would silently proceed without an authenticated user. [NEEDS CLARIFICATION] Whether every route handler across the auth, plans, and planned-items modules consistently checks for a `null` `requireUserId()` result could not be confirmed: the calling route implementations are outside this synthesis node's inputs (it draws only on module-level overview docs, not raw route source), and `docs/modules/planned-items/overview.md` - one of the module-docs named as input to this node - is still an unfilled skeleton (`[UNFILLED]`) in this generation run, so the planned-items module's enforcement pattern in particular is unconfirmed.
- [consistency] This line asserts docs/modules/planned-items/overview.md is 'still an unfilled skeleton ([UNFILLED])', but that document (readable alongside this one) is fully populated with Purpose, Responsibilities, Boundaries, Key Entities, Dependencies and Dependents content - the claim contradicts the current state of the cross-referenced document.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/overview.md, as it currently exists in the docs tree, contains full Purpose/Responsibilities/Boundaries/Key Entities/Dependencies/Dependents content (e.g. 'The Planned Items module manages the individual budget line items ... it currently provides creation of a planned item scoped to a specific plan and user.') - it is not an '[UNFILLED]' skeleton document.
  - location: **Threat (authorization-bypass-by-omission):** `docs/modules/persistence/overview.md` explicitly documents that `requireUserId()` does **not** itself throw or redirect when no session is present - it only returns `null`, "leaving enforcement (e.g. returning a 401) to its callers." A route handler that fails to check for a `null` return would silently proceed without an authenticated user. [NEEDS CLARIFICATION] Whether every route handler across the auth, plans, and planned-items modules consistently checks for a `null` `requireUserId()` result could not be confirmed: the calling route implementations are outside this synthesis node's inputs (it draws only on module-level overview docs, not raw route source), and `docs/modules/planned-items/overview.md` - one of the module-docs named as input to this node - is still an unfilled skeleton (`[UNFILLED]`) in this generation run, so the planned-items module's enforcement pattern in particular is unconfirmed.

## docs/decisions/0002-credentials-provider-with-jwt-sessions.md

- [accuracy] States the JWT session strategy is 'unconfirmed by inputs' and that this section awaits docs/modules/persistence/overview.md becoming available - but that document already exists and already confirms the jwt-strategy session, so the premise for deferring this section is factually incorrect.
  - confidence: high; agreement: 1
  - evidence: docs/modules/persistence/overview.md confirms the jwt-strategy session in its Responsibilities section, so the session strategy is not 'unconfirmed by inputs' as this line claims.
  - location: - [NEEDS CLARIFICATION] Consequences that are specific to a JWT session strategy (e.g. stateless scaling, inability to server-side revoke a session before token expiry, token payload size/claims, refresh behavior) cannot be documented here because the session strategy itself is unconfirmed by inputs. Once `docs/modules/persistence/overview.md` and the `authOptions` configuration are available, this section should be revisited.
- [completeness] This bullet defers JWT-specific consequences pending docs/modules/persistence/overview.md becoming available, but that document is already present and confirms the jwt session strategy plus the jwt/session callbacks - so the Consequences section is missing content it could have included. (location not found inline)
  - confidence: high; agreement: 1
  - evidence: docs/modules/persistence/overview.md (Responsibilities section) confirms 'a `jwt`-strategy session' and 'jwt/session callbacks that propagate the user ID onto the session' as part of authOptions - the same information this Consequences bullet claims is unavailable.
  - location: [NEEDS CLARIFICATION] Consequences that are specific to a JWT session strategy (e.g. stateless scaling, inability to server-side revoke a session before token expiry, token payload size/claims, refresh behavior) cannot be documented here because the session strategy itself is unconfirmed by inputs. Once `docs/modules/persistence/overview.md` and the `authOptions` configuration are available, this section should be revisited.
- [accuracy] Claims docs/modules/persistence/overview.md is 'still an unfilled skeleton document' - it is not. That document is fully populated and directly states authOptions uses a CredentialsProvider verifying email/password against prisma.user with bcrypt.compare and a jwt-strategy session, resolving exactly the gap this line claims is unresolved.
  - confidence: high; agreement: 3
  - evidence: docs/modules/persistence/overview.md (Responsibilities) states: 'Define authOptions (NextAuthOptions): a CredentialsProvider that verifies email/password against prisma.user with bcrypt.compare, a jwt-strategy session, jwt/session callbacks that propagate the user ID onto the session, and a custom /login sign-in page (src/lib/auth.ts).' This file is fully populated, not a skeleton.
  - location: [NEEDS CLARIFICATION] The actual provider configuration inside `authOptions` (imported from `@/lib/auth` in `src/app/api/auth/[...nextauth]/route.ts`) — including the session strategy (JWT vs. database), token lifetime, and the credential-verification logic that would justify the "JWT sessions" half of this decision's title — was out of scope for the `auth` module dispatch and is not established by any input available to this synthesis. `docs/modules/persistence/overview.md`, which per the auth module overview is the document that could resolve this (`@/lib/auth` resolves under `src/lib`, mapped to the `persistence` module in `generation.module-source-map`), is still an unfilled skeleton document (`[UNFILLED]`) as of this generation run, as is `docs/modules/planned-items/overview.md`. `README.md` is generic `create-next-app` boilerplate with no finance-planner-specific content and does not address this decision.
- [completeness] This line claims docs/modules/persistence/overview.md and docs/modules/planned-items/overview.md are 'still an unfilled skeleton document ([UNFILLED])', but both files are fully populated with substantive content (persistence/overview.md even states the jwt-strategy session detail this section says is unresolved), so the Context section is missing available input-evident material rather than genuinely lacking it.
  - confidence: high; agreement: 3
  - evidence: docs/modules/persistence/overview.md exists as a fully-populated document (not marked [UNFILLED]) with Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, and Dependents sections all containing concrete content, including the authOptions/jwt-strategy detail cited above. docs/modules/planned-items/overview.md is likewise fully populated (Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, Dependents all present), not an unfilled skeleton.
  - location: [NEEDS CLARIFICATION] The actual provider configuration inside `authOptions` (imported from `@/lib/auth` in `src/app/api/auth/[...nextauth]/route.ts`) — including the session strategy (JWT vs. database), token lifetime, and the credential-verification logic that would justify the "JWT sessions" half of this decision's title — was out of scope for the `auth` module dispatch and is not established by any input available to this synthesis. `docs/modules/persistence/overview.md`, which per the auth module overview is the document that could resolve this (`@/lib/auth` resolves under `src/lib`, mapped to the `persistence` module in `generation.module-source-map`), is still an unfilled skeleton document (`[UNFILLED]`) as of this generation run, as is `docs/modules/planned-items/overview.md`. `README.md` is generic `create-next-app` boilerplate with no finance-planner-specific content and does not address this decision.
- [consistency] States docs/modules/persistence/overview.md is still an unfilled `[UNFILLED]` skeleton, but the sibling decision doc docs/decisions/0001-credentials-auth-jwt-sessions.md cites that same document multiple times as a substantive, available source of confirmed facts (bcrypt cost factor, jwt-strategy sessions, jwt/session callbacks) - the two decision docs disagree on whether this input was available at generation time.
  - confidence: high; agreement: 3
  - evidence: docs/decisions/0001-credentials-auth-jwt-sessions.md's Context and Decision sections repeatedly cite docs/modules/persistence/overview.md as an available, substantive source (e.g. "Per `docs/modules/persistence/overview.md`, the concrete mechanism is defined in `src/lib/auth.ts` as NextAuth `authOptions`: a `CredentialsProvider` that verifies email/password against `prisma.user` with `bcrypt.compare`, a `jwt`-strategy session..."), whereas this doc (0002) and docs/modules/auth/overview.md both describe docs/modules/persistence/overview.md as an unfilled `[UNFILLED]` skeleton document.
  - location: [NEEDS CLARIFICATION] The actual provider configuration inside `authOptions` (imported from `@/lib/auth` in `src/app/api/auth/[...nextauth]/route.ts`) — including the session strategy (JWT vs. database), token lifetime, and the credential-verification logic that would justify the "JWT sessions" half of this decision's title — was out of scope for the `auth` module dispatch and is not established by any input available to this synthesis. `docs/modules/persistence/overview.md`, which per the auth module overview is the document that could resolve this (`@/lib/auth` resolves under `src/lib`, mapped to the `persistence` module in `generation.module-source-map`), is still an unfilled skeleton document (`[UNFILLED]`) as of this generation run, as is `docs/modules/planned-items/overview.md`. `README.md` is generic `create-next-app` boilerplate with no finance-planner-specific content and does not address this decision.
- [accuracy] Claims no input document states the session.strategy value and that JWT-based sessions are unconfirmed. This is false: docs/modules/persistence/overview.md explicitly describes authOptions as using 'a jwt-strategy session'.
  - confidence: high; agreement: 3
  - evidence: docs/modules/persistence/overview.md (Responsibilities) explicitly states authOptions defines 'a jwt-strategy session', directly stating the session.strategy is JWT-based.
  - location: [NEEDS CLARIFICATION] Whether the session strategy configured in `authOptions` is specifically JWT-based (as opposed to a database-backed session store) is not confirmed by any input in this synthesis's read set. No input document states the `session.strategy` value passed to `NextAuth()`, nor gives a rationale for choosing JWT sessions over the alternative. This must be confirmed against the actual `authOptions` configuration (expected under `src/lib/auth`, owned by the `persistence` module) before this decision record can be considered fully grounded.
- [completeness] The Decision section leaves the JWT session strategy unconfirmed, but docs/modules/persistence/overview.md - already generated and available in the docs tree - explicitly states authOptions configures 'a jwt-strategy session'. This input-evident fact is missing from the Decision section, leaving it incomplete despite being resolvable.
  - confidence: high; agreement: 3
  - evidence: docs/modules/persistence/overview.md (Responsibilities section) states: 'Define `authOptions` (`NextAuthOptions`): a `CredentialsProvider` that verifies email/password against `prisma.user` with `bcrypt.compare`, a `jwt`-strategy session, `jwt`/`session` callbacks that propagate the user ID onto the session, and a custom `/login` sign-in page (`src/lib/auth.ts`).' This is a fully-populated, non-skeleton document (Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, Dependents all present).
  - location: [NEEDS CLARIFICATION] Whether the session strategy configured in `authOptions` is specifically JWT-based (as opposed to a database-backed session store) is not confirmed by any input in this synthesis's read set. No input document states the `session.strategy` value passed to `NextAuth()`, nor gives a rationale for choosing JWT sessions over the alternative. This must be confirmed against the actual `authOptions` configuration (expected under `src/lib/auth`, owned by the `persistence` module) before this decision record can be considered fully grounded.
- [consistency] Contradicts sibling decision doc docs/decisions/0001-credentials-auth-jwt-sessions.md, which states as confirmed (citing docs/modules/persistence/overview.md) that sessions use NextAuth's jwt strategy with jwt/session callbacks propagating the user ID - the same fact this doc (0002) says is unconfirmed by any input.
  - confidence: high; agreement: 3
  - evidence: docs/decisions/0001-credentials-auth-jwt-sessions.md Decision section states: "Sessions use NextAuth's `jwt` strategy rather than database-backed sessions; `jwt` and `session` callbacks propagate the authenticated user's ID onto the session object, per `docs/modules/persistence/overview.md`." This directly resolves, as confirmed, the exact question this doc (0002) declares unconfirmed.
  - location: [NEEDS CLARIFICATION] Whether the session strategy configured in `authOptions` is specifically JWT-based (as opposed to a database-backed session store) is not confirmed by any input in this synthesis's read set. No input document states the `session.strategy` value passed to `NextAuth()`, nor gives a rationale for choosing JWT sessions over the alternative. This must be confirmed against the actual `authOptions` configuration (expected under `src/lib/auth`, owned by the `persistence` module) before this decision record can be considered fully grounded.

## docs/decisions/0003-prisma-postgresql-with-pg-adapter.md

- [consistency] Internal inconsistency: this line claims 'inputs.references = README.md only' and that the adapter package name is unconfirmed, yet the rest of this same document (Context, Decision, Consequences, References) relies heavily on docs/modules/persistence/overview.md as its primary source - and that very file's Dependencies section already names the adapter package as `@prisma/adapter-pg`. The document contradicts itself about which inputs were available and what they establish.
  - confidence: high; agreement: 1
  - evidence: This same document's Context/Decision/Consequences sections quote and cite docs/modules/persistence/overview.md extensively as source material (e.g. 'Per docs/modules/persistence/overview.md, the project uses Prisma as its ORM...' and the References section lists it as 'primary source for the Prisma/PostgreSQL/PrismaPg-adapter implementation described in this decision'). That same referenced file, docs/modules/persistence/overview.md, states in its Dependencies section: 'their imports resolve to external packages (next-auth, bcryptjs, @/generated/prisma/client, @prisma/adapter-pg)' - i.e. it does name the adapter package.
  - location: [NEEDS CLARIFICATION] The exact adapter package name (e.g. `@prisma/adapter-pg`) and the Prisma / adapter version in use are not confirmed: no `package.json` or other build manifest was present in this synthesis node's inputs (`inputs.references` = `README.md` only), and per the dependency-grounding rule a dependency must be confirmed against a manifest, not prose, before being asserted as a version-specific fact.

## docs/executive-summary.md

- [completeness] This bullet claims docs/modules/persistence/overview.md is an unfilled skeleton, but the file is fully populated with concrete responsibilities (Prisma client singleton with globalThis caching and fail-fast on missing DATABASE_URL, NextAuth credentials authOptions with bcrypt.compare, requireUserId() session helper). The Persistence bullet silently omits this available content.
  - confidence: high; agreement: 2
  - evidence: docs/modules/persistence/overview.md exists with populated Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, and Dependents sections (e.g. Prisma client singleton in src/lib/db.ts, authOptions in src/lib/auth.ts, requireUserId() in src/lib/requireUser.ts). No '[UNFILLED]' sentinel appears anywhere in that file. doc_struc.md confirms docs/modules/persistence/overview.md is a declared module-docs input for docs/executive-summary.md.
  - location: - **Persistence.** [NEEDS CLARIFICATION] `docs/modules/persistence/overview.md` is an unfilled skeleton in this generation run. What can be inferred from the other module overviews is that a shared Prisma client (`prisma` from `@/lib/db`) is used for all plan and user CRUD queries; no further detail on the persistence module's own responsibilities is available yet.
- [consistency] docs/modules/persistence/overview.md is fully populated and documents considerably more than 'a shared Prisma client is used for all plan and user CRUD queries' - it also covers the authOptions/CredentialsProvider configuration, the requireUserId() helper, and fail-fast DATABASE_URL behavior, contradicting this doc's claim that 'no further detail ... is available yet'.
  - confidence: high; agreement: 2
  - evidence: docs/modules/persistence/overview.md's Responsibilities section documents: PrismaClient singleton via PrismaPg adapter with globalThis caching, fail-fast on missing DATABASE_URL, authOptions (CredentialsProvider, bcrypt.compare, jwt-strategy session, jwt/session callbacks), and requireUserId(). This is substantially more detail than 'no further detail ... is available yet'.
  - location: - **Persistence.** [NEEDS CLARIFICATION] `docs/modules/persistence/overview.md` is an unfilled skeleton in this generation run. What can be inferred from the other module overviews is that a shared Prisma client (`prisma` from `@/lib/db`) is used for all plan and user CRUD queries; no further detail on the persistence module's own responsibilities is available yet.
- [completeness] This bullet claims docs/modules/planned-items/overview.md is an unfilled skeleton, but the file is fully populated (Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, Dependents; no [UNFILLED] sentinel). The section silently omits the actual planned-items responsibilities (item creation/validation via POST /api/plans/{planId}/items, ownership enforcement) that are available in that input.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/overview.md exists with populated Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, and Dependents sections (e.g. 'The Planned Items module manages the individual budget line items...'; responsibilities list validate/persist via POST /api/plans/{planId}/items). No '[UNFILLED]' sentinel appears anywhere in that file. doc_struc.md (path docs/executive-summary.md, module-docs list) confirms docs/modules/planned-items/overview.md is a declared input for this node.
  - location: - **Plan items.** [NEEDS CLARIFICATION] `docs/modules/planned-items/overview.md` is an unfilled skeleton in this generation run, so the item-level responsibilities (creation, validation, listing under `/api/plans/{planId}/items`) cannot be summarized here beyond what the plans module overview states: that `AddItemForm.tsx` (owned by `plans`) posts to a planned-items endpoint at `/api/plans/{planId}/items`.
- [consistency] docs/modules/planned-items/overview.md is not an unfilled skeleton; it directly documents item creation, Zod validation, and ownership-enforcement responsibilities (and separately flags that no listing route handler was found), so this doc's claim that item-level responsibilities 'cannot be summarized here' beyond the plans-module overview is contradicted by the planned-items overview's own content.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/overview.md contains a fully populated Purpose, Responsibilities (creation, validation via Zod, ownership enforcement via prisma.plan.findFirst), Boundaries, Key Entities, Dependencies, and Dependents sections - not an unfilled skeleton. It also explicitly flags (via its own [NEEDS CLARIFICATION]) that only a POST handler was found, i.e. it does discuss creation/validation/listing scope directly, contrary to this doc's claim that such content 'cannot be summarized here'.
  - location: - **Plan items.** [NEEDS CLARIFICATION] `docs/modules/planned-items/overview.md` is an unfilled skeleton in this generation run, so the item-level responsibilities (creation, validation, listing under `/api/plans/{planId}/items`) cannot be summarized here beyond what the plans module overview states: that `AddItemForm.tsx` (owned by `plans`) posts to a planned-items endpoint at `/api/plans/{planId}/items`.
- [completeness] This Known Gaps entry states both module overviews were unfilled, but both files already contain complete, substantive content. The Known Gaps section is therefore inaccurate about the actual gap and the executive summary is missing the synthesis of real, available planned-items and persistence content.
  - confidence: high; agreement: 1
  - evidence: Both docs/modules/planned-items/overview.md and docs/modules/persistence/overview.md contain full, non-skeleton content (Purpose/Responsibilities/Boundaries/Key Entities/Dependencies/Dependents) with no '[UNFILLED]' sentinel, contradicting the 'were unfilled' premise of this Known Gaps entry.
  - location: - `docs/modules/planned-items/overview.md` and `docs/modules/persistence/overview.md` were unfilled at the time this document was generated; this summary should be regenerated once those module overviews are populated.
- [consistency] Contradicts docs/overview.md and the actual content of docs/modules/persistence/overview.md and docs/modules/planned-items/overview.md: this doc claims both module overviews are unfilled [UNFILLED] skeletons, but both files are fully populated (Purpose/Responsibilities/Boundaries/Key Entities/Dependencies/Dependents), and docs/overview.md's Module Map and Key Characteristics sections directly cite and quote both of them as available sources.
  - confidence: high; agreement: 1
  - evidence: docs/modules/persistence/overview.md and docs/modules/planned-items/overview.md are both fully populated documents (Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, Dependents sections all present with substantive content, no [UNFILLED] sentinel). docs/overview.md lines 29-51 directly cites and quotes both files (module map table with full purpose/source-path rows, dependency edges 'per persistence/overview.md' and 'per planned-items/overview.md'), showing a peer synthesis document treats both as available and populated.
  - location: finance-planner is a Next.js App Router web application that lets an authenticated user manage monthly financial plans: register/sign in, create a plan for a given year and month, add and list items within a plan, view a plan's detail with a running total, and delete a plan. This summary synthesizes the module-level `overview.md` documents produced for `auth`, `plans`, and `web-ui`, plus `README.md`, per the inputs declared for this node. [NEEDS CLARIFICATION] `docs/modules/planned-items/overview.md` and `docs/modules/persistence/overview.md`, both declared as inputs for this node, are still unfilled skeleton documents (`[UNFILLED]` sentinel) as of this generation run and could not be incorporated; the planned-items and persistence module summaries below are correspondingly incomplete.

## docs/glossary.md

- [completeness] The glossary claims docs/modules/planned-items/domain-model.md, docs/modules/planned-items/data-model.md, and docs/modules/persistence/data-model.md are unfilled skeletons, but all three now contain full generated content (a detailed `PlannedItem` entity with invariants/relationships, and a `user` table definition). The glossary is missing terms genuinely available from its own named inputs, e.g. `PlannedItem` as a distinct domain term (with its `categoryId`/nullable-Category note and creation invariant) beyond the partial 'Item' entry sourced only from the plans module.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/domain-model.md and docs/modules/planned-items/data-model.md are both fully generated documents (front matter generated_from: 39f2f753a6dbde2c5b34c6281adeeab77b7a87d5) with substantive, non-skeleton content defining the `PlannedItem` entity (attributes, invariants, relationships, ER diagram). docs/modules/persistence/data-model.md is likewise fully generated (generated_from: f42c2544c64f0a192045ac5c7a007738e9faaa58) with a `user` table definition (id, email, password, name columns). None of the three contain the `[UNFILLED]` skeleton sentinel.
  - location: [NEEDS CLARIFICATION] The following module-output documents named as inputs to this glossary have not yet been generated (their content is the `[UNFILLED]` skeleton sentinel), so no terms could be sourced from them: `docs/modules/planned-items/domain-model.md`, `docs/modules/planned-items/data-model.md`, and `docs/modules/persistence/data-model.md`. Once those modules are generated, this glossary should be regenerated or extended to include their domain and persistence terminology (e.g. planned-item lifecycle terms and the persistence layer's shared data-access vocabulary).
- [consistency] glossary.md claims docs/modules/planned-items/domain-model.md, docs/modules/planned-items/data-model.md, and docs/modules/persistence/data-model.md are unfilled [UNFILLED] skeleton stubs, but all three currently contain full generated content (PlannedItem entity/table definitions, the persistence module's user table, etc.) in the same generation batch - the glossary's 'Terms not yet available' claim contradicts the current state of those docs and omits terms (e.g. PlannedItem, planId, note/categoryId nullability, the persistence user table) that are now sourceable from them.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/domain-model.md and docs/modules/planned-items/data-model.md both contain full generated content (Overview, Class Diagram, Entities/Tables sections, etc., not an [UNFILLED] sentinel), and docs/modules/persistence/data-model.md likewise contains full generated content (PostgreSQL/Prisma overview, user table, etc.). All three share the same generated_inputs hash (sha256:68be7ab88910ec0e327eb9a9a2cf9f641568123d13e77fa409d08720e56f824d) as glossary.md itself, i.e. they belong to the same generation batch and are not skeleton stubs.
  - location: [NEEDS CLARIFICATION] The following module-output documents named as inputs to this glossary have not yet been generated (their content is the `[UNFILLED]` skeleton sentinel), so no terms could be sourced from them: `docs/modules/planned-items/domain-model.md`, `docs/modules/planned-items/data-model.md`, and `docs/modules/persistence/data-model.md`. Once those modules are generated, this glossary should be regenerated or extended to include their domain and persistence terminology (e.g. planned-item lifecycle terms and the persistence layer's shared data-access vocabulary).

## docs/integration-tests/authorization-isolation.md

- [consistency] The Notes section repeats the stale claim that docs/modules/planned-items/overview.md is an unfilled skeleton limiting this document's authorization-behavior coverage, but that module doc is fully populated and already documents ownership enforcement for planned-item creation.
  - confidence: high; agreement: 1
  - evidence: docs/modules/planned-items/overview.md's Responsibilities section states plan-ownership enforcement is implemented for item creation, contradicting the claim that this document's ability to describe authorization behavior for plan-items endpoints is limited by an unfilled planned-items overview.
  - location: - `docs/modules/planned-items/overview.md` is still an unfilled skeleton
- [consistency] This document asserts docs/modules/planned-items/overview.md is an unfilled skeleton and that ownership enforcement for item-level endpoints could not be confirmed, but that referenced document is fully filled and explicitly states 'Enforce that a planned item can only be created under a plan owned by the authenticated user' - directly contradicting the claim.
  - confidence: high; agreement: 1
  - evidence: docs/modules/planned-items/overview.md is not an unfilled skeleton: it contains full Purpose, Responsibilities ('Enforce that a planned item can only be created under a plan owned by the authenticated user.'), Boundaries, Key Entities, Dependencies, and Dependents sections.
  - location: | planned-items | [NEEDS CLARIFICATION] `docs/modules/planned-items/overview.md`, named as a module-doc input for this node, is still an unfilled skeleton document (`[UNFILLED]`) as of this generation run; whether/how item-level endpoints (`/api/plans/{planId}/items`) enforce plan ownership could not be confirmed here. |

## docs/modules/persistence/behavior/credentials-authorization.md

- [consistency] Internal inconsistency: the section heading cites 'Steps 5-11' but the section's own prose describes the successful bcrypt-match/return-user-object path, which is steps 12-13 in the autonumbered sequence diagram above - the step range should extend to 13 to cover the content actually described.
  - confidence: high; agreement: 1
  - evidence: Counting the mermaid `autonumber` sequence messages in the same doc's diagram (lines 39-57): 1=authorize call, 2=email normalize, 3=password default, 4=return null (missing), 5=findUnique, 6=null, 7=return null, 8=user, 9=bcrypt.compare, 10=false, 11=return null, 12=true, 13=return {id, email, name}. The success path (bcrypt true -> return user object) that section 3's prose explicitly describes ('Only a `true` result returns a user object ... to NextAuth') corresponds to steps 12-13, which fall outside the cited 'Steps 5-11' range.
  - location: ### 3. User lookup and password check (Steps 5-11)

## docs/modules/persistence/behavior/prisma-client-singleton-initialization.md

- [consistency] The step range 'Steps 3-8' is internally inconsistent with the document's own mermaid diagram: step 3 is actually the DATABASE_URL-unset throw (belongs to the 'Environment check' section, not adapter/client reuse), while step 9 - the 'construct a new [client] otherwise' case this section's text describes - is excluded from the cited range.
  - confidence: high; agreement: 1
  - evidence: Counting the mermaid autonumbered messages in the same document (lines 32-63): message 3 is 'DB-->>Mod: throw Error("DATABASE_URL is not set")' (the DATABASE_URL-unset branch, which section 1 'Environment check' already covers), and message 9 is 'DB->>Client: new PrismaClient({ adapter, log: ["error","warn"] })' - the 'construct a new one otherwise' case this same section's prose describes - which falls outside the cited 3-8 range.
  - location: ### 2. Adapter/client reuse (Steps 3-8)
- [consistency] Section 3 labels the conditional cache write-back as 'Step 9', but by the diagram's own autonumbering the cache-write message ('DB->>Global: cache prisma & prismaAdapter') is step 10, not step 9 (step 9 is the 'new PrismaClient(...)' construction message).
  - confidence: high; agreement: 1
  - evidence: Per the same mermaid diagram's autonumbering, message 9 is 'DB->>Client: new PrismaClient(...)' and the conditional cache write-back 'DB->>Global: cache prisma & prismaAdapter' inside the 'alt NODE_ENV !== "production"' block is message 10.
  - location: ### 3. Conditional caching (Step 9)

## docs/modules/persistence/data-model.md

- [completeness] The Tables section only documents the `user` accessor and states the module's inputs are limited to src/lib/auth.ts, src/lib/db.ts, and src/lib/requireUser.ts. It omits the `plan` and `plannedItem` Prisma model accessors, which are directly evidenced in this doc's own code inputs (prisma.plan.findMany/create/findFirst/delete in plans routes, prisma.plannedItem.create in the items route) with concrete fields (userId, year, month, currency, title, createdAt for plan; planId, title, amountCents, categoryId, note for plannedItem). This input-evident material is silently missing from the Tables section.
  - confidence: high; agreement: 1
  - evidence: src/app/api/plans/route.ts uses prisma.plan.findMany/create with fields userId, year, month, currency, title; src/app/api/plans/[planId]/route.ts uses prisma.plan.findFirst/delete with an `items` relation ordered by createdAt; src/app/api/plans/[planId]/items/route.ts uses prisma.plannedItem.create with fields planId, title, amountCents, categoryId, note. These are part of this dispatch's codeFiles but no plan/plannedItem table is documented.
  - location: ### Tables

## docs/modules/persistence/domain-model.md

- [completeness] The Entities section documents only User, and the Overview asserts User is 'the one domain concept it references directly'. But the input set given to this doc includes src/app/api/plans/route.ts, src/app/api/plans/[planId]/route.ts, and src/app/api/plans/[planId]/items/route.ts, which use prisma.plan and prisma.plannedItem extensively (fields userId, year, month, currency, title / planId, title, amountCents, categoryId, note). Plan and PlannedItem (and a categoryId-referenced Category) are domain entities evidenced by these inputs but are entirely missing from the Entities section.
  - confidence: high; agreement: 1
  - evidence: src/app/api/plans/route.ts calls prisma.plan.findMany/prisma.plan.create with fields userId, year, month, currency, title; src/app/api/plans/[planId]/route.ts calls prisma.plan.findFirst/prisma.plan.delete and includes { items: ... }; src/app/api/plans/[planId]/items/route.ts calls prisma.plannedItem.create with fields planId, title, amountCents, categoryId, note - all four files are listed in this doc's codeFiles input set.
  - location: This module (`src/lib/`) does not include its own Prisma schema among the files available to it; it wires a shared database client and authentication configuration for the rest of the application. The one domain concept it references directly is the authenticated **User**, via `prisma.user` lookups in `src/lib/auth.ts` and the session identity resolved by `src/lib/requireUser.ts`.

## docs/modules/persistence/overview.md

- [accuracy] The doc claims confirming the 401-enforcing callers is outside this module's manifest scope, but src/app/api/plans/route.ts (one of this doc's own code inputs) directly shows requireUserId() being checked and a 401 returned - the fact was verifiable from the inputs actually provided.
  - confidence: high; agreement: 1
  - evidence: src/app/api/plans/route.ts GET/POST both call `const userId = await requireUserId(); if (!userId) return NextResponse.json({ error: "Unauthorized" }, { status: 401 });` - this exact enforcement pattern is directly visible in a file listed among this doc's own code inputs, contradicting the claim that confirming it is out of scope.
  - location: - Throw or redirect when no session is present; `requireUserId()` only returns `null`, leaving enforcement (e.g. returning a 401) to its callers. [NEEDS CLARIFICATION] The calling API routes that enforce this are outside this module's manifest scope.
- [accuracy] The doc asserts it has no visibility into consumers of authOptions/prisma/requireUserId and that inspecting src/app/api/** is out of scope, but src/app/api/** and src/app/**/page.tsx files were themselves provided among this doc's code inputs and plainly show these imports - the Dependents table is inaccurate about its own available evidence.
  - confidence: high; agreement: 2
  - evidence: Grep over the code inputs shows authOptions imported in src/app/page.tsx, src/app/plans/page.tsx, src/app/plans/[planId]/page.tsx, src/app/api/auth/[...nextauth]/route.ts; prisma imported in src/app/api/auth/register/route.ts, src/app/api/plans/route.ts, src/app/api/plans/[planId]/route.ts, src/app/api/plans/[planId]/items/route.ts, src/app/plans/page.tsx, src/app/plans/[planId]/page.tsx; requireUserId imported in src/app/api/plans/route.ts, src/app/api/plans/[planId]/route.ts, src/app/api/plans/[planId]/items/route.ts. All of these files are among this doc's own code inputs.
  - location: | [NEEDS CLARIFICATION] | This module's inputs (`src/lib/auth.ts`, `src/lib/db.ts`, `src/lib/requireUser.ts`) give no visibility into which other modules import `authOptions`, `prisma`, or `requireUserId` - confirming consumers requires inspecting `src/app/api/**` and other modules' source, which is outside this module's manifest scope. |
- [consistency] The Dependents row claims no visibility into which modules consume `authOptions`/`prisma`/`requireUserId`, but the auth, plans, and planned-items module overviews (visible in the same doc set) each explicitly list `persistence` as a dependency and name exactly what they use it for (Prisma client for CRUD, `requireUserId()`/`authOptions` for session resolution), so the consumer information this row says is unavailable is already documented elsewhere in the generated docs.
  - confidence: medium; agreement: 2
  - evidence: docs/modules/auth/overview.md Dependencies table: '| persistence (`@/lib/db` / `prisma`) | User lookup (`prisma.user.findUnique`) and creation (`prisma.user.create`) during registration. |'; docs/modules/plans/overview.md Dependencies table: '| persistence | Provides the Prisma client (`@/lib/db`) used for all plan CRUD queries |' and '| auth | Resolves the current user for both API routes (`requireUserId()`) ... |'; docs/modules/planned-items/overview.md Dependencies table: '| persistence | Database access via the shared Prisma client (`@/lib/db`) |'.
  - location: | [NEEDS CLARIFICATION] | This module's inputs (`src/lib/auth.ts`, `src/lib/db.ts`, `src/lib/requireUser.ts`) give no visibility into which other modules import `authOptions`, `prisma`, or `requireUserId` - confirming consumers requires inspecting `src/app/api/**` and other modules' source, which is outside this module's manifest scope. |

## docs/modules/persistence/technical-concerns.md

- [accuracy] The claim that 'no structured error-response JSON is constructed in this module' is incorrect: multiple API routes in this module's codeFiles (register, plans, plans/[planId], items) construct structured `{ error, details? }` JSON responses with explicit HTTP status codes.
  - confidence: high; agreement: 1
  - evidence: src/app/api/auth/register/route.ts, src/app/api/plans/route.ts, src/app/api/plans/[planId]/route.ts and src/app/api/plans/[planId]/items/route.ts all return `NextResponse.json({ error: "..." , details? }, { status: ... })` for validation/auth/not-found failures (e.g. `{ error: "Invalid input", details: parsed.error.flatten() }`, `{ error: "Unauthorized" }`, `{ error: "Not found" }`, `{ error: "Email already exists" }`).
  - location: [NEEDS CLARIFICATION] `authorize()` communicates validation failure only by returning `null` (which NextAuth surfaces as a generic sign-in failure); no structured error-response JSON is constructed in this module.
- [accuracy] Claim is contradicted by the inputs: register/route.ts, plans/route.ts and items/route.ts all use Zod schemas with explicit length/format/range constraints (email(), min/max on strings and numbers), so field-level validation rules beyond the auth presence check do exist in this module's inputs.
  - confidence: high; agreement: 1
  - evidence: src/app/api/auth/register/route.ts defines `RegisterSchema = z.object({ email: z.string().email(), password: z.string().min(8, ...), name: z.string().min(1).max(80).optional() })`; src/app/api/plans/route.ts defines `CreatePlanSchema` with `.min(2000).max(2100)`, `.min(1).max(12)`, `.min(3).max(3)`; src/app/api/plans/[planId]/items/route.ts defines `CreateItemSchema` with `.min(1).max(120)`, `.int().min(0)`, `.max(400)`. All of these are format/length/range field-level rules and are part of this module's codeFiles.
  - location: [NEEDS CLARIFICATION] Beyond the presence check described above, no field-level rules (format, length, range) are present in this module's inputs.

## docs/modules/persistence/use-cases/resolve-session-user.md

- [accuracy] The doc frames the HTTP 401 response as a hypothetical example 'outside this module's inputs', but this exact behavior is concretely implemented in codeFiles (e.g. src/app/api/plans/route.ts), so the caveat is inaccurate given the available inputs.
  - confidence: medium; agreement: 2
  - evidence: The same three route.ts files respond to a falsy requireUserId() with `return NextResponse.json({ error: "Unauthorized" }, { status: 401 })`, which is exactly the caller behavior described as speculative/outside-scope.
  - location: **Continues at:** ends - caller must decide how to respond (e.g. return HTTP 401); that logic is outside this module's inputs
- [completeness] The Alternative Flow states the HTTP-401 handling logic is 'outside this module's inputs', but the exact 401 response pattern is implemented in three caller files that are part of this doc's codeFiles (src/app/api/plans/route.ts, [planId]/route.ts, [planId]/items/route.ts) - the doc omits this readily available, input-evident detail.
  - confidence: high; agreement: 2
  - evidence: src/app/api/plans/route.ts lines 14-16 and 27-29 (`if (!userId) return NextResponse.json({ error: "Unauthorized" }, { status: 401 });`), mirrored in src/app/api/plans/[planId]/route.ts and src/app/api/plans/[planId]/items/route.ts, all listed in codeFiles.
  - location: **Continues at:** ends - caller must decide how to respond (e.g. return HTTP 401); that logic is outside this module's inputs
- [accuracy] The doc states concrete callers of requireUserId() are 'outside this module's inputs', but the module's own codeFiles list includes three route handlers (plans/route.ts, plans/[planId]/route.ts, plans/[planId]/items/route.ts) that call requireUserId() directly, contradicting this claim.
  - confidence: high; agreement: 2
  - evidence: src/app/api/plans/route.ts calls `const userId = await requireUserId();` in both GET and POST and returns `NextResponse.json({ error: "Unauthorized" }, { status: 401 })` when falsy; the same pattern appears in src/app/api/plans/[planId]/route.ts and src/app/api/plans/[planId]/items/route.ts. All three files are listed in this doc's codeFiles input.
  - location: | Server-side caller | Primary | Any server code that calls `await requireUserId()`; concrete callers are outside this module's inputs |
- [completeness] The Actors table claims concrete callers of requireUserId() are outside this module's inputs, but three callers (src/app/api/plans/route.ts, src/app/api/plans/[planId]/route.ts, src/app/api/plans/[planId]/items/route.ts) are in the provided codeFiles and each call requireUserId() directly - this input-evident material is omitted from the Actors description.
  - confidence: high; agreement: 2
  - evidence: src/app/api/plans/route.ts (GET/POST, lines 13-16 and 26-29), src/app/api/plans/[planId]/route.ts, and src/app/api/plans/[planId]/items/route.ts are all in this doc's codeFiles and each imports requireUserId and, on a falsy userId, returns NextResponse.json({ error: "Unauthorized" }, { status: 401 }) - i.e. concrete callers and the exact HTTP-401 handling the doc hedges about are present in the module's inputs.
  - location: | Server-side caller | Primary | Any server code that calls `await requireUserId()`; concrete callers are outside this module's inputs |

## docs/modules/web-ui/behavior/server-client-rendering.md

- [accuracy] The doc claims whether client components exist under /plans and /login is 'outside the web-ui module's manifest scope reviewed for this document', but LoginClient.tsx (used by /login) and NewPlanForm.tsx (used by /plans) are both in this dispatch's codeFiles and both begin with "use client" - the answer (yes, client components exist under both destination routes) is directly evidenced by inputs already in scope, not out of scope.
  - confidence: high; agreement: 2
  - evidence: src/app/login/LoginClient.tsx line 1 is "use client"; and it is rendered by src/app/login/page.tsx under the /login route. src/app/plans/NewPlanForm.tsx line 1 is also "use client"; and is rendered by src/app/plans/page.tsx under the /plans route. Both LoginClient.tsx and NewPlanForm.tsx are listed in this dispatch's codeFiles (and in docs/.expanded-inputs.json's code[] array), so they are within scope, not outside it.
  - location: Neither `src/app/layout.tsx` nor `src/app/page.tsx` contains a `"use client"` directive, so both render as Server Components — no client-side rendering occurs in this observed flow. [NEEDS CLARIFICATION] Whether client components exist under the destination routes (`/plans`, `/login`) is outside the web-ui module's manifest scope reviewed for this document.
- [completeness] This behavior doc is titled 'Server Client Rendering' but covers only the trivial root-route redirect and disclaims client-side rendering as 'outside the web-ui module's manifest scope'. That claim is contradicted by the dispatch's own codeFiles, which include five 'use client' components (LoginClient.tsx, register/page.tsx, NewPlanForm.tsx, AddItemForm.tsx, DeletePlanButton.tsx) squarely inside the web-ui module. The document omits any discussion of the client-side rendering half of its own title despite that material being available in its inputs.
  - confidence: high; agreement: 2
  - evidence: codeFiles for this dispatch include src/app/login/LoginClient.tsx, src/app/register/page.tsx, src/app/plans/NewPlanForm.tsx, src/app/plans/[planId]/AddItemForm.tsx, and src/app/plans/[planId]/DeletePlanButton.tsx, each containing a "use client" directive (grep-confirmed). These client components are within the web-ui module's manifest scope (module-source-map web-ui: src/app in docs/doc_struc.md) and were explicitly provided as inputs to this document.
  - location: Neither `src/app/layout.tsx` nor `src/app/page.tsx` contains a `"use client"` directive, so both render as Server Components — no client-side rendering occurs in this observed flow. [NEEDS CLARIFICATION] Whether client components exist under the destination routes (`/plans`, `/login`) is outside the web-ui module's manifest scope reviewed for this document.
- [accuracy] The doc claims authOptions (src/lib/auth.ts) is 'outside this dispatch's web-ui manifest scope' and 'its content was not reviewed here', but src/lib/auth.ts is explicitly included in this dispatch's codeFiles and its full content (JWT session strategy, CredentialsProvider with bcrypt check, pages.signIn: "/login") is readable. The doc also mislabels src/lib as 'the persistence module' when src/lib/db.ts (the Prisma client), not src/lib/auth.ts, is the persistence code.
  - confidence: high; agreement: 2
  - evidence: src/lib/auth.ts is listed in this dispatch's codeFiles and in docs/.expanded-inputs.json's code[] array (role: code, sizeBytes 1293), so its content (NextAuthOptions with JWT session strategy, CredentialsProvider, pages.signIn) is directly readable and was reviewed as part of this dispatch. Additionally, src/lib/db.ts (the Prisma client) is the actual persistence module in src/lib; src/lib/auth.ts is an auth-configuration module, not 'the persistence module'.
  - location: | `authOptions` misconfiguration | [NEEDS CLARIFICATION] `authOptions` is defined in the persistence module (`src/lib`, via `@/lib/auth`), which is outside this dispatch's web-ui manifest scope; its content was not reviewed here. |
- [completeness] The Error Scenarios row claims authOptions content 'was not reviewed here' because it is 'outside this dispatch's web-ui manifest scope', but src/lib/auth.ts is directly listed in this document's own codeFiles input and defines authOptions in full (credentials provider, jwt/session callbacks, signIn page). The document leaves this error scenario unaddressed despite the source being available to it.
  - confidence: high; agreement: 2
  - evidence: src/lib/auth.ts is listed in this dispatch's codeFiles and defines authOptions (NextAuthOptions) with a CredentialsProvider, jwt/session callbacks, and a pages.signIn config - fully available content, not outside the dispatch's scope.
  - location: | `authOptions` misconfiguration | [NEEDS CLARIFICATION] `authOptions` is defined in the persistence module (`src/lib`, via `@/lib/auth`), which is outside this dispatch's web-ui manifest scope; its content was not reviewed here. |

## docs/modules/web-ui/technical-concerns.md

- [accuracy] States no field validation rules are present, but zod schemas in the module's own API route inputs define concrete field validation rules (email format, password length, numeric ranges, string length limits).
  - confidence: high; agreement: 2
  - evidence: src/app/api/auth/register/route.ts RegisterSchema (email format, password min(8), name min/max); src/app/api/plans/route.ts CreatePlanSchema (title max(80), year 2000-2100, month 1-12, currency exactly 3 chars); src/app/api/plans/[planId]/items/route.ts CreateItemSchema (title min/max, amountCents int min(0), note max(400)).
  - location: [NEEDS CLARIFICATION] No entity or field validation rules are present in the reviewed scope.
- [completeness] Field Validation Rules section says none are present, but this document's codeFiles contain concrete zod field-level rules (email format, password min length, string length/range bounds) that are absent from the doc.
  - confidence: high; agreement: 2
  - evidence: Zod schemas define concrete field rules that are evidenced but omitted: RegisterSchema.email (z.string().email()), RegisterSchema.password (min 8, custom message 'Minimalne 8 znaku') in src/app/api/auth/register/route.ts; CreatePlanSchema.year/month ranges and currency length in src/app/api/plans/route.ts; CreateItemSchema.title max 120, amountCents min 0, note max 400 in src/app/api/plans/[planId]/items/route.ts.
  - location: [NEEDS CLARIFICATION] No entity or field validation rules are present in the reviewed scope.
- [accuracy] Claims no error-response shape is evidenced, but the module's API route inputs consistently use a `{ error: string }` (optionally with `details`) JSON error shape.
  - confidence: high; agreement: 2
  - evidence: src/app/api/auth/register/route.ts, src/app/api/plans/route.ts, src/app/api/plans/[planId]/route.ts, and src/app/api/plans/[planId]/items/route.ts all return NextResponse.json({ error: string, ...}) with status codes 400/401/404/409/201.
  - location: [NEEDS CLARIFICATION] No error-response shape is evidenced in `src/app/layout.tsx` or `src/app/page.tsx`.
- [completeness] Error Format section claims no error-response shape is evidenced, but the API route files in this document's own codeFiles show a consistent `{ error, details? }` JSON error shape used across register, plans, and items routes.
  - confidence: high; agreement: 2
  - evidence: src/app/api/plans/route.ts, src/app/api/auth/register/route.ts, and src/app/api/plans/[planId]/items/route.ts all consistently return NextResponse.json({ error: string }, { status }) on failure, and { error: string, details: parsed.error.flatten() } specifically for zod validation failures - a concrete, repeated error-response shape.
  - location: [NEEDS CLARIFICATION] No error-response shape is evidenced in `src/app/layout.tsx` or `src/app/page.tsx`.
- [accuracy] Claims the web-ui module's reviewed scope has no user-input surface, but the module's own code inputs (AddItemForm.tsx, NewPlanForm.tsx, LoginClient.tsx, and three API routes with zod schemas) contain extensive form inputs and request-body validation logic. The document only checked layout.tsx/page.tsx and ignored the rest of the module's provided code files.
  - confidence: high; agreement: 2
  - evidence: src/app/plans/[planId]/AddItemForm.tsx, src/app/plans/NewPlanForm.tsx, src/app/login/LoginClient.tsx all render <form> elements with controlled inputs; src/app/api/auth/register/route.ts defines a zod RegisterSchema, src/app/api/plans/route.ts defines a zod CreatePlanSchema, and src/app/api/plans/[planId]/items/route.ts defines a zod CreateItemSchema, each validated via safeParse on request bodies.
  - location: | [NEEDS CLARIFICATION] | No form inputs, request bodies, or validation logic appear in `src/app/layout.tsx` or `src/app/page.tsx`; this module's reviewed scope contains no user-input surface. |
- [completeness] Validation Layers section restricts its scope statement to only layout.tsx/page.tsx and concludes no user-input surface exists, but this document's own codeFiles include multiple request-body zod schemas (CreatePlanSchema, RegisterSchema, CreateItemSchema) and client-side form validation (required/minLength attributes, custom amount parser) that were never covered.
  - confidence: high; agreement: 2
  - evidence: src/app/api/plans/route.ts defines CreatePlanSchema (zod) with year/month/currency/title rules; src/app/api/auth/register/route.ts defines RegisterSchema with email/password(min 8)/name rules; src/app/api/plans/[planId]/items/route.ts defines CreateItemSchema with title/amountCents/note rules. Client-side, src/app/register/page.tsx uses required and minLength={8} on inputs, src/app/login/LoginClient.tsx uses required email/password inputs, and src/app/plans/[planId]/AddItemForm.tsx implements a regex-based amount-to-cents validator. All of these files are listed in this document's own codeFiles input.
  - location: | [NEEDS CLARIFICATION] | No form inputs, request bodies, or validation logic appear in `src/app/layout.tsx` or `src/app/page.tsx`; this module's reviewed scope contains no user-input surface. |
- [accuracy] The Common Error Codes table claims no error codes are evidenced, but the module's API routes evidence concrete status codes (400, 401, 404, 409, 201) with specific error messages.
  - confidence: high; agreement: 2
  - evidence: Observed status codes across the module's API routes: 400 'Invalid input' (register, plans, items routes), 401 'Unauthorized' (plans, plans/[planId], items routes), 404 'Not found' (plans/[planId], items routes), 409 'Email already exists' / 'Plan for this month already exists' (register, plans routes), 201 Created (register, plans, items routes).
  - location: | [NEEDS CLARIFICATION] | Not evidenced in the reviewed scope |
- [completeness] Common Error Codes table says nothing is evidenced, but the API route files in this document's own codeFiles evidence specific status codes (400, 401, 404, 409) with distinct meanings that are omitted from the table.
  - confidence: high; agreement: 2
  - evidence: Concrete status codes with meanings appear across codeFiles: 401 'Unauthorized' (src/app/api/plans/route.ts, src/app/api/plans/[planId]/items/route.ts), 400 'Invalid input' on zod parse failure (same files plus register/route.ts), 404 'Not found' (items/route.ts), 409 'Plan for this month already exists' / 'Email already exists' (plans/route.ts, register/route.ts).
  - location: | [NEEDS CLARIFICATION] | Not evidenced in the reviewed scope |

## docs/operations/infrastructure.md

- [completeness] The doc claims docs/modules/persistence/overview.md and docs/modules/planned-items/overview.md were 'still unfilled skeleton documents ([UNFILLED] sentinel, no generated content)', but both files are fully generated with substantive content. This false premise causes the Data Storage, Authentication Infrastructure, and Environment Configuration sections below to omit persistence-derived facts (PostgreSQL engine, JWT session strategy, DATABASE_URL requirement) that were actually available in the doc tree.
  - confidence: high; agreement: 2
  - evidence: docs/modules/persistence/overview.md is fully generated content (Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, Dependents sections all populated - e.g. 'Instantiate and cache a single PrismaClient bound to PostgreSQL through a PrismaPg adapter' and 'Fail fast at module load if DATABASE_URL is not set'), not an '[UNFILLED]' sentinel document. docs/modules/planned-items/overview.md is likewise fully populated (Purpose/Responsibilities/Boundaries/Key Entities/Dependencies/Dependents), not unfilled.
  - location: [NEEDS CLARIFICATION] `docs/modules/persistence/overview.md` and
- [consistency] infrastructure.md asserts docs/modules/persistence/overview.md and docs/modules/planned-items/overview.md were still unfilled skeleton documents (`[UNFILLED]` sentinel) at generation time, but both documents are fully generated with substantive content in the current docs tree, contradicting the claim and leaving stale unresolved markers for facts (database engine, session strategy) that are actually confirmed elsewhere in the tree.
  - confidence: high; agreement: 2
  - evidence: docs/modules/persistence/overview.md and docs/modules/planned-items/overview.md both contain full generated content (Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, Dependents sections), not an `[UNFILLED]` sentinel.
  - location: [NEEDS CLARIFICATION] `docs/modules/persistence/overview.md` and
- [completeness] The database engine is directly named as PostgreSQL in docs/modules/persistence/overview.md and docs/modules/persistence/deployment.md, contradicting the claim here that 'the concrete database engine ... [is] not confirmed by this node's inputs'.
  - confidence: high; agreement: 1
  - evidence: docs/modules/persistence/overview.md, Responsibilities: 'Instantiate and cache a single PrismaClient bound to PostgreSQL through a PrismaPg adapter'; docs/modules/persistence/deployment.md External Dependencies table: 'PostgreSQL | Primary data store, accessed via Prisma with a PrismaPg adapter'.
  - location: [NEEDS CLARIFICATION] The concrete database engine, hosting/managed-service
- [accuracy] The document claims docs/modules/persistence/overview.md 'is still an unfilled skeleton' and that the session strategy 'could not be confirmed for this infrastructure document,' but persistence/overview.md (a declared input of this document) is fully populated and explicitly states the session uses a 'jwt-strategy', contradicting the claim that this could not be confirmed.
  - confidence: high; agreement: 2
  - evidence: docs/modules/persistence/overview.md (one of this document's own declared inputs, per its Summary section) is fully populated, not an [UNFILLED] skeleton, and explicitly states: 'Define authOptions (NextAuthOptions): a CredentialsProvider that verifies email/password against prisma.user with bcrypt.compare, a jwt-strategy session, jwt/session callbacks that propagate the user ID onto the session...' This is corroborated by docs/decisions/0001-credentials-auth-jwt-sessions.md, which cites docs/modules/persistence/overview.md as confirming the jwt session strategy.
  - location: is still an unfilled skeleton, the session strategy (JWT vs. database-backed
- [consistency] infrastructure.md states the session strategy (JWT vs. database-backed) could not be confirmed because docs/modules/persistence/overview.md was an unfilled skeleton, but that document is not a skeleton - it explicitly states authOptions uses a jwt-strategy session, directly resolving the claimed gap.
  - confidence: high; agreement: 2
  - evidence: docs/modules/persistence/overview.md, Responsibilities section, states: "Define `authOptions` (`NextAuthOptions`): a `CredentialsProvider` that verifies email/password against `prisma.user` with `bcrypt.compare`, a `jwt`-strategy session, `jwt`/`session` callbacks that propagate the user ID onto the session..."
  - location: is still an unfilled skeleton, the session strategy (JWT vs. database-backed
- [consistency] infrastructure.md claims no available module overview states the underlying database engine, but docs/modules/persistence/overview.md (which infrastructure.md itself lists as a declared input) explicitly states the Prisma client is bound to PostgreSQL via a PrismaPg adapter.
  - confidence: high; agreement: 1
  - evidence: docs/modules/persistence/overview.md, Responsibilities section, states: "Instantiate and cache a single `PrismaClient` bound to PostgreSQL through a `PrismaPg` adapter, reused across Next.js hot reloads via a `globalThis` cache (`src/lib/db.ts`)." infrastructure.md itself names docs/modules/persistence/overview.md as a declared input for this node (see its own text: "`docs/modules/persistence/overview.md` and `docs/modules/planned-items/overview.md`, both declared as inputs for this node").
  - location: node state which underlying database engine Prisma is configured against.

## docs/operations/runbooks/backup-restore.md

- [consistency] The document states that docs/modules/planned-items/overview.md 'is still an unfilled skeleton (`[UNFILLED]`) as of this generation run and could not be incorporated', but that file is in fact fully filled out with populated Purpose/Responsibilities/Boundaries/Dependencies sections and contains no [UNFILLED] marker - contradicting this claim.
  - confidence: high; agreement: 1
  - evidence: docs/modules/planned-items/overview.md contains full, non-skeleton content (Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, Dependents sections all populated) with no '[UNFILLED]' marker anywhere in the file.
  - location: [NEEDS CLARIFICATION] `docs/modules/planned-items/overview.md`, named as a module-doc

## docs/operations/runbooks/rotate-auth-secret.md

- [accuracy] The line asserts authOptions' 'internal configuration is not established by this dispatch's inputs', but docs/modules/persistence/overview.md - named in this doc's own Summary as one of the module overviews grounding this dispatch - does establish authOptions' internal configuration (CredentialsProvider, jwt session strategy, callbacks, custom login page), contradicting the claim of non-establishment.
  - confidence: medium; agreement: 2
  - evidence: docs/modules/persistence/overview.md documents authOptions' internal configuration in detail: CredentialsProvider verifying against prisma.user via bcrypt.compare, jwt-strategy session, jwt/session callbacks propagating the user ID, and a custom /login sign-in page (src/lib/auth.ts).
  - location: - `authOptions` — the object that would configure the credentials provider and any JWT/session signing secret — is imported from `@/lib/auth`, which falls outside the `auth` module's own read scope (it maps to the `persistence` / `src/lib` scope per the project's `generation.module-source-map`). Its internal configuration is not established by this dispatch's inputs.
- [consistency] This doc asserts authOptions's internal configuration is 'not established by this dispatch's inputs,' but the sibling docs/modules/persistence/overview.md (already generated in the docs tree) does establish it: a CredentialsProvider verifying via bcrypt.compare with a jwt-strategy session defined in src/lib/auth.ts.
  - confidence: medium; agreement: 2
  - evidence: docs/modules/persistence/overview.md Responsibilities section: "Define `authOptions` (`NextAuthOptions`): a `CredentialsProvider` that verifies email/password against `prisma.user` with `bcrypt.compare`, a `jwt`-strategy session ... (`src/lib/auth.ts`)." This establishes authOptions's internal configuration (provider type, verification method, session strategy).
  - location: - `authOptions` — the object that would configure the credentials provider and any JWT/session signing secret — is imported from `@/lib/auth`, which falls outside the `auth` module's own read scope (it maps to the `persistence` / `src/lib` scope per the project's `generation.module-source-map`). Its internal configuration is not established by this dispatch's inputs.
- [accuracy] This line claims docs/modules/persistence/overview.md is 'still an unfilled skeleton' and that JWT-vs-database session strategy is unconfirmed, but persistence/overview.md (one of the five module overviews this dispatch's own Summary says grounded it) has substantive content confirming a jwt-strategy session in src/lib/auth.ts, and docs/modules/persistence/deployment.md independently confirms the same 'jwt session strategy'. The claim of unresolved/unfilled status is factually stale relative to sibling docs.
  - confidence: high; agreement: 3
  - evidence: docs/modules/persistence/overview.md (generated_branch sdlc/20260925-1148) is NOT an unfilled skeleton - it states in its Responsibilities section: 'Define `authOptions` (`NextAuthOptions`): a `CredentialsProvider` that verifies email/password against `prisma.user` with `bcrypt.compare`, a `jwt`-strategy session, `jwt`/`session` callbacks... (`src/lib/auth.ts`)'. This is corroborated by docs/modules/persistence/deployment.md: '`src/lib/auth.ts` configures NextAuth (`CredentialsProvider`, `jwt` session strategy)'.
  - location: [NEEDS CLARIFICATION] Whether NextAuth is configured here with the JWT session strategy (in which case rotating a signing secret invalidates all existing sessions, forcing every user to re-authenticate) or the database session strategy (in which case rotation impact differs) is unconfirmed — the module doc that would resolve this, `docs/modules/persistence/overview.md`, is still an unfilled skeleton (`[UNFILLED]`) as of this generation run.
- [completeness] This section's premise (that docs/modules/persistence/overview.md is still an unfilled skeleton and cannot resolve the session-strategy question) is stale: docs/modules/persistence/overview.md is now filled and explicitly states the session strategy is JWT (as does docs/modules/persistence/deployment.md). The 'Impact of rotation' section (which depends on this same unresolved question) is therefore incomplete - it omits input-evident material (JWT strategy => rotating the signing secret invalidates all existing sessions) that is available elsewhere in the generated docs tree.
  - confidence: high; agreement: 3
  - evidence: docs/modules/persistence/overview.md line 25 states authOptions is 'a jwt-strategy session' defined in src/lib/auth.ts, and docs/modules/persistence/deployment.md line 37 likewise states 'src/lib/auth.ts configures NextAuth (CredentialsProvider, jwt session strategy)'. Both are filled, non-skeleton documents in the same docs/ tree.
  - location: [NEEDS CLARIFICATION] Whether NextAuth is configured here with the JWT session strategy (in which case rotating a signing secret invalidates all existing sessions, forcing every user to re-authenticate) or the database session strategy (in which case rotation impact differs) is unconfirmed — the module doc that would resolve this, `docs/modules/persistence/overview.md`, is still an unfilled skeleton (`[UNFILLED]`) as of this generation run.
- [consistency] This doc claims docs/modules/persistence/overview.md is still an unfilled [UNFILLED] skeleton that leaves the JWT-vs-database session strategy unconfirmed, but the actual docs/modules/persistence/overview.md in the tree is fully generated and explicitly states the session uses a jwt-strategy with a CredentialsProvider verifying via bcrypt.compare - contradicting this doc's premise and unresolved-clarification framing.
  - confidence: high; agreement: 3
  - evidence: docs/modules/persistence/overview.md (generated, not the [UNFILLED] sentinel) states under Responsibilities: "Define `authOptions` (`NextAuthOptions`): a `CredentialsProvider` that verifies email/password against `prisma.user` with `bcrypt.compare`, a `jwt`-strategy session, `jwt`/`session` callbacks that propagate the user ID onto the session, and a custom `/login` sign-in page (`src/lib/auth.ts`)." This directly answers the JWT-vs-database session-strategy question and shows persistence/overview.md is not an unfilled skeleton.
  - location: [NEEDS CLARIFICATION] Whether NextAuth is configured here with the JWT session strategy (in which case rotating a signing secret invalidates all existing sessions, forcing every user to re-authenticate) or the database session strategy (in which case rotation impact differs) is unconfirmed — the module doc that would resolve this, `docs/modules/persistence/overview.md`, is still an unfilled skeleton (`[UNFILLED]`) as of this generation run.

## docs/operations/runbooks/vercel-deploy.md

- [consistency] This doc claims docs/modules/planned-items/overview.md is 'an unfilled skeleton document ([UNFILLED])', but that file is actually fully populated with a Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, and Dependents section describing the POST /api/plans/{planId}/items handler - contradicting the claim and leaving a verification step unresolved that could actually be grounded from that overview.
  - confidence: high; agreement: 1
  - evidence: docs/modules/planned-items/overview.md is a fully-populated module overview (Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, Dependents sections all contain concrete content, no [UNFILLED] sentinel present anywhere in the file).
  - location: - [ ] [NEEDS CLARIFICATION] The [planned-items module overview](../../modules/planned-items/overview.md) named as an input to this node is, as of this generation run, an unfilled skeleton document (`[UNFILLED]`), so no plan-item-related verification step could be grounded here.

## docs/quality-attributes.md

- [completeness] Error Handling section claims both source docs are unfilled, but docs/modules/planned-items/technical-concerns.md documents the same flat `{ error, details }` shape (`'Invalid input'`) and docs/modules/persistence/technical-concerns.md documents that authorize() returns null with no structured error JSON - both are available and should have been synthesized, including into the 'converges on the same observed shape' analysis above it.
  - confidence: high; agreement: 1
  - evidence: docs/modules/planned-items/technical-concerns.md lines 42-55 (Error Format / Common Error Codes: the same flat `{ error, details }` shape, `'Invalid input'`); docs/modules/persistence/technical-concerns.md lines 44-50 (Error Format: authorize() returns null, no structured error JSON).
  - location: - **Planned-items, Persistence**: [NEEDS CLARIFICATION] `modules/planned-items/technical-concerns.md` and `modules/persistence/technical-concerns.md` are unfilled — no content available to synthesize.
- [completeness] Deployment & Infrastructure section claims modules/persistence/deployment.md is unfilled, but that file contains full Configuration (required DATABASE_URL), Health Checks, Resource Requirements, Scaling, and External Dependencies sections - this content exists and was not synthesized despite being exactly the kind of material this section requests (database hosting, connection info).
  - confidence: high; agreement: 2
  - evidence: docs/modules/persistence/deployment.md lines 17-63: Configuration (required `DATABASE_URL` env var), Health Checks, Resource Requirements, Scaling, and External Dependencies (PostgreSQL via Prisma/PrismaPg) sections are all authored.
  - location: [NEEDS CLARIFICATION] `modules/persistence/deployment.md` is unfilled — no deployment or infrastructure content is available to synthesize a persistence-layer deployment quality attribute (e.g. database hosting, migration strategy, connection pooling, backup/restore posture).
- [consistency] docs/modules/persistence/deployment.md is not unfilled - it documents DATABASE_URL/NODE_ENV configuration, secrets, health checks, and PostgreSQL as an external dependency - contradicting this document's claim that no persistence deployment content is available.
  - confidence: high; agreement: 2
  - evidence: docs/modules/persistence/deployment.md contains a fully populated Configuration/Environment Variables table (DATABASE_URL, NODE_ENV), a Secrets subsection, a Health Checks subsection, and an External Dependencies table listing PostgreSQL - real content, not an unfilled placeholder.
  - location: [NEEDS CLARIFICATION] `modules/persistence/deployment.md` is unfilled — no deployment or infrastructure content is available to synthesize a persistence-layer deployment quality attribute (e.g. database hosting, migration strategy, connection pooling, backup/restore posture).
- [consistency] This instruction to wait for authoring is moot: the three referenced module documents are already authored with substantive content, so this synthesis is stale rather than blocked on future authoring.
  - confidence: high; agreement: 1
  - evidence: docs/modules/planned-items/technical-concerns.md, docs/modules/persistence/technical-concerns.md, and docs/modules/persistence/deployment.md already contain full authored content as read directly, so no regeneration is needed to obtain their quality-attribute content.
  - location: [NEEDS CLARIFICATION] Regenerate this quality-attributes synthesis after those three module documents are authored, so that caching, data-validation, error-handling, internationalization, and deployment attributes can be reported for the planned-items and persistence modules.
- [completeness] Cross-Module Gaps section states these three module documents were not yet generated / present only as [UNFILLED] skeleton content, but all three exist with full authored content - the premise driving this entire section (and the closing regeneration instruction) is incorrect, and the document's coverage of caching/validation/error-handling/i18n/deployment for planned-items and persistence is incomplete as a result.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/technical-concerns.md, docs/modules/persistence/technical-concerns.md, and docs/modules/persistence/deployment.md all contain full authored sections (Caching, Data Validation, Internationalization, and/or Configuration/External Dependencies), not [UNFILLED] skeleton placeholders.
  - location: The following module-output documents named as inputs to this synthesis were not yet generated (present only as `[UNFILLED]` skeleton content) at the time of this run:
- [consistency] This Cross-Module Gaps claim is contradicted by the actual state of the three listed files, which already contain full authored content rather than '[UNFILLED]' skeletons.
  - confidence: high; agreement: 2
  - evidence: The three named files (docs/modules/planned-items/technical-concerns.md, docs/modules/persistence/technical-concerns.md, docs/modules/persistence/deployment.md) each contain substantive authored sections rather than '[UNFILLED]' skeleton content, so they were already generated at the time this synthesis ran.
  - location: The following module-output documents named as inputs to this synthesis were not yet generated (present only as `[UNFILLED]` skeleton content) at the time of this run:
- [completeness] Summary claims 3 of 6 source docs were not yet authored / are [UNFILLED], but docs/modules/planned-items/technical-concerns.md, docs/modules/persistence/technical-concerns.md, and docs/modules/persistence/deployment.md all exist on disk with full, non-skeleton content covering caching, data validation, i18n, and deployment - the synthesis omitted available source material for the whole document.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/technical-concerns.md (full Caching/Data Validation/Internationalization sections, lines 14-59), docs/modules/persistence/technical-concerns.md (full Caching/Data Validation/Internationalization sections, lines 17-58), and docs/modules/persistence/deployment.md (full Configuration/Health Checks/External Dependencies sections, lines 17-63) all exist with authored content, not [UNFILLED] skeletons.
  - location: This document synthesizes cross-module quality-attribute findings (caching, data validation, error handling, internationalization, and deployment) from the per-module `technical-concerns.md` and `deployment.md` documents already generated for the finance-planner project. Three of the six source documents (`modules/planned-items/technical-concerns.md`, `modules/persistence/technical-concerns.md`, and `modules/persistence/deployment.md`) had not yet been authored at the time this synthesis ran — they are marked `[UNFILLED]` in the doc tree — so this document cannot yet report quality attributes for the planned-items and persistence modules. Those gaps are flagged below rather than filled with assumptions.
- [consistency] Contradicted by the actual linked documents: docs/modules/persistence/technical-concerns.md, docs/modules/persistence/deployment.md, and docs/modules/planned-items/technical-concerns.md all contain full authored content (not '[UNFILLED]' skeletons), so this doc's premise that they were unauthored and its resulting caching/validation/i18n/deployment gaps for planned-items and persistence are stale/incorrect.
  - confidence: high; agreement: 2
  - evidence: docs/modules/persistence/technical-concerns.md and docs/modules/persistence/deployment.md both contain full, substantive sections (Caching, Data Validation, Internationalization for technical-concerns.md; Environment Variables, Secrets, Health Checks, External Dependencies for deployment.md) - they are not '[UNFILLED]' skeletons. docs/modules/planned-items/technical-concerns.md likewise contains full Caching, Data Validation (with a PlannedItem field-rules table), and Internationalization sections, not an '[UNFILLED]' skeleton.
  - location: This document synthesizes cross-module quality-attribute findings (caching, data validation, error handling, internationalization, and deployment) from the per-module `technical-concerns.md` and `deployment.md` documents already generated for the finance-planner project. Three of the six source documents (`modules/planned-items/technical-concerns.md`, `modules/persistence/technical-concerns.md`, and `modules/persistence/deployment.md`) had not yet been authored at the time this synthesis ran — they are marked `[UNFILLED]` in the doc tree — so this document cannot yet report quality attributes for the planned-items and persistence modules. Those gaps are flagged below rather than filled with assumptions.
- [completeness] Caching table row for Persistence claims the source doc is unfilled, but docs/modules/persistence/technical-concerns.md's Caching section reports 'Caching Enabled: No' and documents a client-instance caching pattern in src/lib/db.ts - this content exists and was not synthesized into the table.
  - confidence: high; agreement: 1
  - evidence: docs/modules/persistence/technical-concerns.md lines 17-27 (Caching section: 'Caching Enabled: No', and description of the PrismaClient/PrismaPg globalForPrisma instance-caching pattern in src/lib/db.ts).
  - location: | Persistence | [NEEDS CLARIFICATION] `modules/persistence/technical-concerns.md` is unfilled — no content available to synthesize. |
- [completeness] Data Validation table row for Planned-items claims the source doc is unfilled, but docs/modules/planned-items/technical-concerns.md documents the `CreateItemSchema` Zod validation and a full field-rules table - this content exists and was omitted from the synthesis.
  - confidence: high; agreement: 1
  - evidence: docs/modules/planned-items/technical-concerns.md lines 22-55 (Data Validation section with a Zod `CreateItemSchema` validation-layers table and a full Field Validation Rules table for title/amountCents/categoryId/note).
  - location: | Planned-items | [NEEDS CLARIFICATION] `modules/planned-items/technical-concerns.md` is unfilled — no content available to synthesize. | — |
- [accuracy] The quoted regex `^(\d+)(\.(\d{1,2})?)?$` misquotes the actual code. The real regex in AddItemForm.tsx is `^(\d+)(\.(\d{1,2}))?$` (no `?` after `(\d{1,2})`). The doc's version adds an extra `?` making the fractional digits themselves optional within the decimal-point group, which would incorrectly accept inputs like "123." that the real regex rejects.
  - confidence: high; agreement: 1
  - evidence: src/app/plans/[planId]/AddItemForm.tsx line 25: `const m = normalized.match(/^(\d+)(\.(\d{1,2}))?$/);` - the decimal-digits group `(\d{1,2})` is NOT itself optional; only the whole `(\.(\d{1,2}))` group (a literal dot followed by 1-2 digits) is optional.
  - location: | Plans | Zod schema (`CreatePlanSchema`) validates `title`, `year`, `month`, `currency` on `POST /api/plans`; `AddItemForm.tsx` validates the amount format client-side with a regex (`^(\d+)(\.(\d{1,2})?)?$`). Database-level constraint enforcement is `[NEEDS CLARIFICATION]` (inferred only from `409` handling, not from the schema itself). | `modules/plans/technical-concerns.md` |

## docs/requirements/functional-account-access.md

- [completeness] FR-3's [NEEDS CLARIFICATION] says the session strategy (JWT vs. database-backed) is unconfirmed because persistence's overview document is unfilled. docs/modules/persistence/overview.md is present and filled, and explicitly states the session uses a jwt strategy - this answer is missing from FR-3.
  - confidence: high; agreement: 1
  - evidence: docs/modules/persistence/overview.md (Responsibilities): "... a `jwt`-strategy session, `jwt`/`session` callbacks that propagate the user ID onto the session ..." - present and filled, resolving the JWT-vs-database-backed session-strategy question this doc marks unconfirmed.
  - location: (`persistence`) has an unfilled overview document; these details are
- [consistency] FR-3's clarification says the session strategy (JWT vs. database-backed) is unconfirmed because persistence's overview was unfilled, but docs/modules/persistence/overview.md is now filled and states the session uses a jwt-strategy - contradicting this doc's 'unconfirmed' claim for the strategy portion.
  - confidence: high; agreement: 1
  - evidence: docs/modules/persistence/overview.md (Responsibilities) explicitly states authOptions defines "a jwt-strategy session", directly answering the JWT-vs-database question this requirements doc marks unconfirmed.
  - location: [NEEDS CLARIFICATION] The session strategy (JWT vs. database-backed) and token
- [consistency] FR-5's clarification says whether planned-items operations are scoped to the owning account could not be confirmed because docs/modules/planned-items/overview.md was an unfilled skeleton, but that overview is now filled and explicitly confirms planned-item creation is scoped to plans owned by the authenticated user - contradicting the claimed unconfirmed status.
  - confidence: high; agreement: 1
  - evidence: docs/modules/planned-items/overview.md (Responsibilities) states: "Enforce that a planned item can only be created under a plan owned by the authenticated user." This overview is now filled and present in the docs tree, not the unfilled skeleton this requirements doc describes.
  - location: [NEEDS CLARIFICATION] Whether `planned-items` operations (adding/listing items
- [consistency] FR-2's clarification claims the credential-verification logic cannot be confirmed because docs/modules/persistence/overview.md was an unfilled skeleton, but that overview is now filled and explicitly states the CredentialsProvider verifies email/password via bcrypt.compare - contradicting the 'cannot be confirmed here' claim.
  - confidence: high; agreement: 1
  - evidence: docs/modules/persistence/overview.md (Responsibilities) now states: "Define authOptions (NextAuthOptions): a CredentialsProvider that verifies email/password against prisma.user with bcrypt.compare, a jwt-strategy session, jwt/session callbacks that propagate the user ID onto the session, and a custom /login sign-in page (src/lib/auth.ts)." This is a filled, non-skeleton document already present in the docs tree this requirements doc synthesizes module overviews from.
  - location: module-source-map, and `docs/modules/persistence/overview.md` was still an
- [consistency] The Summary states the planned-items and persistence module overviews were still unfilled `[UNFILLED]` skeletons and could not be incorporated, but both docs/modules/persistence/overview.md and docs/modules/planned-items/overview.md are now fully filled documents elsewhere in the same docs tree, contradicting this doc's account of what was available.
  - confidence: high; agreement: 1
  - evidence: docs/modules/persistence/overview.md and docs/modules/planned-items/overview.md are both present in the docs tree with full non-skeleton content (Purpose/Responsibilities/Boundaries/Dependencies sections), not the `[UNFILLED]` sentinel this requirements doc's Summary describes.
  - location: overviews declared as inputs for this document were still unfilled skeleton
- [completeness] FR-2's [NEEDS CLARIFICATION] says the credential-verification logic can't be confirmed because docs/modules/persistence/overview.md was 'still an unfilled skeleton at generation time'. That file is currently present and fully filled, and states the CredentialsProvider verifies the password with bcrypt.compare - this content is available and missing from FR-2.
  - confidence: high; agreement: 1
  - evidence: docs/modules/persistence/overview.md (Responsibilities): "Define `authOptions` (`NextAuthOptions`): a `CredentialsProvider` that verifies email/password against `prisma.user` with `bcrypt.compare` ..." - this file is present and fully filled (not an [UNFILLED] skeleton), and it directly answers the credential-verification question this doc marks unconfirmed.
  - location: unfilled skeleton at generation time, so this requirement's verification step

## docs/risks.md

- [accuracy] Repeats the same inaccurate 'unfilled' claim in the Testing Risk Summary: both docs/modules/planned-items/testing.md and docs/modules/persistence/testing.md are fully authored (e.g. planned-items/testing.md lists concrete happy-path and authorization/validation-failure test cases), so testing risk for these modules could in fact be assessed from available inputs.
  - confidence: high; agreement: 3
  - evidence: docs/modules/planned-items/testing.md and docs/modules/persistence/testing.md both contain full 'Strategy Overview', 'Test Levels', 'Test Categories', 'Key Test Scenarios', 'Test Data', 'CI/CD Integration', and 'Test Environment' sections with concrete content grounded in the respective modules' route handlers/lib files - they are not unfilled.
  - location: - **planned-items / persistence**: [NEEDS CLARIFICATION] Testing risk cannot be assessed; `docs/modules/planned-items/testing.md` and `docs/modules/persistence/testing.md` are unfilled.
- [completeness] The Testing Risk Summary declares planned-items/persistence testing risk entirely unassessable, but items/route.ts (a provided codeFile) exposes concrete testable branches (auth failure, ownership failure, validation failure, happy path) that mirror the level of detail given for auth/plans/web-ui elsewhere in this same document, so this section under-covers material that was available.
  - confidence: medium; agreement: 3
  - evidence: src/app/api/plans/[planId]/items/route.ts is a provided codeFile that fully documents the planned-items module's request/response behavior (Zod schema, auth guard, ownership check, status codes), so a testing-risk assessment for planned-items specifically was derivable from inputs actually given to this review, contrary to the blanket 'cannot be assessed' claim.
  - location: - **planned-items / persistence**: [NEEDS CLARIFICATION] Testing risk cannot be assessed; `docs/modules/planned-items/testing.md` and `docs/modules/persistence/testing.md` are unfilled.
- [consistency] This line states the planned-items and persistence testing.md files are unfilled, but both files contain full authored content (test levels, categories, key scenarios, etc.), contradicting the claim and the parallel claim in the Documentation Coverage Gap section.
  - confidence: high; agreement: 3
  - evidence: docs/modules/planned-items/testing.md and docs/modules/persistence/testing.md are both fully authored (Strategy Overview, Test Levels, Test Categories, Key Test Scenarios, Test Data, CI/CD Integration, Test Environment sections all present with content), not unfilled.
  - location: - **planned-items / persistence**: [NEEDS CLARIFICATION] Testing risk cannot be assessed; `docs/modules/planned-items/testing.md` and `docs/modules/persistence/testing.md` are unfilled.
- [accuracy] Claims the planned-items and persistence technical-concerns.md/testing.md files 'had not been authored' and 'each contains only the [UNFILLED] sentinel'. This is factually wrong: all four files exist and are fully authored with substantive, module-specific content (caching, validation layers, field rules, error formats, test levels/categories/scenarios, etc.), not a bare sentinel.
  - confidence: high; agreement: 3
  - evidence: docs/modules/planned-items/technical-concerns.md and docs/modules/planned-items/testing.md are fully authored (headed sections: Caching, Data Validation, Error Format, Internationalization / Strategy Overview, Test Levels, Test Categories, Key Test Scenarios, Test Data, CI/CD Integration, Test Environment, each with substantive content). docs/modules/persistence/technical-concerns.md and docs/modules/persistence/testing.md are likewise fully authored with the same section structure. None of these four files consists solely of an '[UNFILLED]' sentinel; they contain many concrete, grounded statements alongside occasional inline [NEEDS CLARIFICATION] notes on specific sub-points.
  - location: [NEEDS CLARIFICATION] The `technical-concerns.md` and `testing.md` documents for the **planned-items** and **persistence** modules (`docs/modules/planned-items/technical-concerns.md`, `docs/modules/planned-items/testing.md`, `docs/modules/persistence/technical-concerns.md`, `docs/modules/persistence/testing.md`) had not been authored at the time this synthesis was produced (each contains only the `[UNFILLED]` sentinel). No risk content could be derived for these two modules. This document should be regenerated once those module docs are filled in.
- [completeness] Risk Register omits planned-items and persistence entirely, citing missing module docs, but the actual source code for both modules (items/route.ts, db.ts, requireUser.ts) is present among this doc's own code inputs, so risk-relevant facts (e.g. no caching in db.ts, flat non-coded error shape in items/route.ts, DATABASE_URL-unset failure mode) could have been derived directly from code rather than left as an unfilled gap.
  - confidence: high; agreement: 3
  - evidence: The codeFiles enumerated as inputs for this doc review include src/app/api/plans/[planId]/items/route.ts (the planned-items POST handler: Zod CreateItemSchema with title/amountCents/categoryId/note rules, plan-ownership check, 401/404/400/201 branches) and src/lib/db.ts plus src/lib/requireUser.ts (the persistence/session-resolution code: PrismaClient/PrismaPg singleton, DATABASE_URL-unset throw, getServerSession-based user resolution). This directly contradicts the document's own line-17 claim that 'No additional source code or reference material was read to produce this synthesis' and its line-21 claim that 'No risk content could be derived for these two modules' - source material for both planned-items and persistence was available in the review inputs.
  - location: [NEEDS CLARIFICATION] The `technical-concerns.md` and `testing.md` documents for the **planned-items** and **persistence** modules (`docs/modules/planned-items/technical-concerns.md`, `docs/modules/planned-items/testing.md`, `docs/modules/persistence/technical-concerns.md`, `docs/modules/persistence/testing.md`) had not been authored at the time this synthesis was produced (each contains only the `[UNFILLED]` sentinel). No risk content could be derived for these two modules. This document should be regenerated once those module docs are filled in.
- [consistency] risks.md's Documentation Coverage Gap claims the planned-items and persistence technical-concerns.md/testing.md files each contain only an `[UNFILLED]` sentinel and were not authored, but those four files (readable in docs/modules/) are fully authored with substantive Caching/Data Validation/Testing/CI-CD sections - this contradicts the other generated docs in the same doc set.
  - confidence: high; agreement: 3
  - evidence: docs/modules/planned-items/technical-concerns.md and docs/modules/planned-items/testing.md contain full substantive sections (Caching, Data Validation with a Field Validation Rules table, Error Format, Common Error Codes, Internationalization; Strategy Overview, Test Levels, Test Categories, Key Test Scenarios, Test Data, CI/CD Integration, Test Environment) with per-claim [NEEDS CLARIFICATION] annotations, not a single [UNFILLED] sentinel. Likewise docs/modules/persistence/technical-concerns.md and docs/modules/persistence/testing.md contain full Caching/Data Validation/Internationalization and Strategy Overview/Test Levels/Key Test Scenarios/CI-CD/Test Environment sections. None of the four files contains the literal string "[UNFILLED]".
  - location: [NEEDS CLARIFICATION] The `technical-concerns.md` and `testing.md` documents for the **planned-items** and **persistence** modules (`docs/modules/planned-items/technical-concerns.md`, `docs/modules/planned-items/testing.md`, `docs/modules/persistence/technical-concerns.md`, `docs/modules/persistence/testing.md`) had not been authored at the time this synthesis was produced (each contains only the `[UNFILLED]` sentinel). No risk content could be derived for these two modules. This document should be regenerated once those module docs are filled in.
- [accuracy] R4 asserts the server-side re-validation claim is 'unconfirmed' because planned-items documentation 'is not yet available', but docs/modules/planned-items/technical-concerns.md is fully authored and does document server-side validation (Zod CreateItemSchema, amountCents integer/minimum 0), so this risk's premise is inaccurate.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/technical-concerns.md and docs/modules/planned-items/testing.md exist and are fully authored (see above), so the premise that 'the planned-items module's own technical-concerns/testing documentation is not yet available' is false; server-side re-validation of amountCents is in fact addressed by docs/modules/planned-items/technical-concerns.md's Field Validation Rules table (amountCents: number (int), Yes, integer, minimum 0).
  - location: | R4 | Client-side item-amount validation in the plans module (`AddItemForm.tsx` regex `^(\d+)(\.(\d{1,2})?)?$`) is stated to be re-validated server-side in the planned-items module, but the planned-items module's own technical-concerns/testing documentation is not yet available (see Documentation Coverage Gap above), so this server-side re-validation claim is unconfirmed from this synthesis's inputs. | plans, planned-items | Medium | `docs/modules/plans/technical-concerns.md`: "This is client-side only in this module's files; server-side re-validation of the amount happens in the planned-items module, outside this manifest." |
- [consistency] R4 asserts the planned-items module's technical-concerns/testing docs are unavailable to confirm server-side amount re-validation, but docs/modules/planned-items/technical-concerns.md is authored and documents an `amountCents` Zod validation rule (integer, minimum 0), contradicting the 'not yet available' premise.
  - confidence: high; agreement: 2
  - evidence: docs/modules/planned-items/technical-concerns.md's Field Validation Rules table documents `amountCents | number (int) | Yes | integer, minimum 0` under a Zod schema (`CreateItemSchema`), directly addressing server-side amount re-validation - contradicting the claim that this module's documentation is unavailable to confirm the point.
  - location: | R4 | Client-side item-amount validation in the plans module (`AddItemForm.tsx` regex `^(\d+)(\.(\d{1,2})?)?$`) is stated to be re-validated server-side in the planned-items module, but the planned-items module's own technical-concerns/testing documentation is not yet available (see Documentation Coverage Gap above), so this server-side re-validation claim is unconfirmed from this synthesis's inputs. | plans, planned-items | Medium | `docs/modules/plans/technical-concerns.md`: "This is client-side only in this module's files; server-side re-validation of the amount happens in the planned-items module, outside this manifest." |
