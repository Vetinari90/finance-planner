{
  "schemaVersion": "1",
  "rel": "docs/operations/runbooks/rotate-auth-secret.md",
  "dimension": "completeness",
  "findings": [
    {
      "confidence": "high",
      "evidence": "docs/modules/persistence/overview.md line 25 states authOptions is 'a jwt-strategy session' defined in src/lib/auth.ts, and docs/modules/persistence/deployment.md line 37 likewise states 'src/lib/auth.ts configures NextAuth (CredentialsProvider, jwt session strategy)'. Both are filled, non-skeleton documents in the same docs/ tree.",
      "locationHint": "[NEEDS CLARIFICATION] Whether NextAuth is configured here with the JWT session strategy (in which case rotating a signing secret invalidates all existing sessions, forcing every user to re-authenticate) or the database session strategy (in which case rotation impact differs) is unconfirmed — the module doc that would resolve this, `docs/modules/persistence/overview.md`, is still an unfilled skeleton (`[UNFILLED]`) as of this generation run.",
      "message": "This section's premise (that docs/modules/persistence/overview.md is still an unfilled skeleton and cannot resolve the session-strategy question) is stale: docs/modules/persistence/overview.md is now filled and explicitly states the session strategy is JWT (as does docs/modules/persistence/deployment.md). The 'Impact of rotation' section (which depends on this same unresolved question) is therefore incomplete - it omits input-evident material (JWT strategy => rotating the signing secret invalidates all existing sessions) that is available elsewhere in the generated docs tree."
    },
    {
      "confidence": "medium",
      "evidence": "docs/operations/runbooks/secret-rotation.md is a sibling runbook covering the same general topic (secret rotation procedure, checklist, verification, rollback), and docs/modules/persistence/overview.md / docs/modules/persistence/deployment.md are the module docs that establish the JWT session strategy underlying this runbook's open questions.",
      "locationHint": "- [Auth module overview](../../modules/auth/overview.md)",
      "message": "The 'Related' section links only to the auth module overview, omitting docs/operations/runbooks/secret-rotation.md (a directly related, same-topic runbook) and docs/modules/persistence/overview.md / deployment.md (the docs that resolve the session-strategy question this runbook flags as unconfirmed), leaving the cross-reference set incomplete."
    }
  ]
}
