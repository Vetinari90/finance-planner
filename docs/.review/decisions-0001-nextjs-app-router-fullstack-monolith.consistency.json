{
  "schemaVersion": "1",
  "rel": "docs/decisions/0001-nextjs-app-router-fullstack-monolith.md",
  "dimension": "consistency",
  "findings": [
    {
      "confidence": "high",
      "evidence": "docs/modules/auth/overview.md contains no mention of `requireUserId()` anywhere in its Purpose, Responsibilities, Boundaries, Key Entities, Dependencies, or Dependents sections. Only docs/modules/planned-items/overview.md raises the requireUserId()-ownership ambiguity: '[NEEDS CLARIFICATION] `requireUserId()` (`@/lib/requireUser`) performs session/user resolution; per `generation.module-source-map` this file path falls under the `persistence` module (`src/lib`), though functionally it is an authentication concern; the true owning module could not be confirmed from this dispatch's inputs.'",
      "locationHint": "- Module boundaries are enforced only by convention (the project's `generation.module-source-map`) rather than by physical/deployment separation. Several module overviews flag ownership ambiguity that stems directly from this: `requireUserId()` lives under `src/lib` (mapped to the persistence module) despite being functionally an authentication concern, per [docs/modules/auth/overview.md](../modules/auth/overview.md) and [docs/modules/planned-items/overview.md](../modules/planned-items/overview.md).",
      "message": "This bullet attributes the requireUserId()-ownership-ambiguity observation to both docs/modules/auth/overview.md and docs/modules/planned-items/overview.md, but auth/overview.md never mentions requireUserId() or flags this ambiguity anywhere in its text - only planned-items/overview.md does. The citation to auth/overview.md is unsupported by that document's actual content."
    }
  ]
}
